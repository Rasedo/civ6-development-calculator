# Engine audit — open work

THIS FILE IS A LIST OF OPEN WORK. A resolved entry is DELETED, not
annotated; what was fixed, when and why is the git log's job (`git log -S
<id>` finds an entry's history). One weight and one OPEN list per entry;
every bullet is one of four things and says which:

- **BUILD** — buildable now from a sourced rule; names the symbols.
- **BLOCKER** — waits on another entry; names it.
- **ASK** — the source under-determines it; the owner rules (the ledger below).
- **LAB** — the live game answers it (`tools/civ6lab/SESSION5.md`, scene named).
  A rule the install carries no number for is inferred from what the live game
  does: a scene that varies its inputs and fits the observed outcome.

**RULES (owner):**
- Anchor code BY SYMBOL, never by line number.
- VERIFY-BEFORE-IMPLEMENT against a real Civ 6 source: the owner's install
  (XML over civilopedia; Base <- Exp1 <- Exp2, take the last) first, the
  live game second, forums last. An unsourced magnitude is an ASK, never an
  invention.
- A gap deferred on an unbuilt mechanic is TWO open items: the mechanic and
  the gap naming it. "Recorded" and "descoped" are deferrals, never closures.
- Every new mechanic records which lane REACHES it; a green gate over an
  unreached mechanic proves nothing.
- When an entry closes, delete its row and its entry in the SAME commit.

**State:** P8 training PARKED until this file is empty.

## Open weight

Hand-weighted 1–8 by the size of what is LEFT to build. One row per entry,
no row without an entry. The rows are the source of truth; the subtotals
are their sum, and `python tools/audit_totals_check.py` (battery stage 0)
re-adds them.

| Open item | Weight | What is left |
|---|---|---|
| **A. Engine vs engine** | **0** | |
| B-24r governor tails | 1 | Foreign Investor and Affluence wait on C-38; the Industrialist's reach and the plants' carbon are readings (LAB) |
| B-31r trade-route tails | 1 | the plunder payout's law (LAB, measured: not flat, not era-only); the destination's free choice (P8) |
| B-D unsourced data values | 1 | k's confirmation and the rounding, Anarchy's length, the per-city war-weariness split, the durations and "Standard Speed" amounts online (LAB) |
| B-82 the turn-limit score winner | 1 | the Palace's 2, pillaged buildings, Religion's Converted term, the tie direction (LAB) |
| B-86 the emergency combat terms' reach | 1 | the lost Nuclear emergency's -1, the burst's attacker side, raising an emergency (LAB) |
| B-89 fire on a religious unit | 1 | an air strike on a civilian, a shot at a lone Support unit (LAB) |
| B-91 a religion's beliefs | 1 | Religious Colonization's pressure, Holy Waters' reach, "cities following", whose religion a worship building follows (LAB) |
| B-93 Great People as Gathering Storm layers them | 1 | whether Darwin counts the plot underfoot (LAB) |
| B-94 an improvement's and a wonder's ground | 1 | the generator's ResetTerrain and natural cliffs on wonder plots (BUILD); the ice radius (ASK); the unpublished multi-plot shapes, a water wonder on a lake (LAB) |
| B-95 a city centre's strength | 1 | the base's "ever built" mid-game (LAB) |
| **B. Fidelity vs real Civ 6** | **10** | |
| C-1 power | 1 | the building rows' shape, the CATASTROPHIC band, the unexplained MINOR and ring losses (LAB) |
| C-2 diplomatic agreements | 2 | the settle promise's measured charges, the standalone near-settle grievance deleted (BUILD) |
| C-16 the spy's second half | 1 | the counterspy's escape term (LAB, blocked on a second human seat) |
| C-20 the route's transportation efficiency | 1 | the Trader's walk and range against the game's pathfinder (LAB) |
| C-26 civilization abilities, the residue | 1 | the struck unit's other terms (audit) |
| C-34 air combat's second half | 1 | an interception's XP, war weariness and draw order, a Priority Target under a patrol (LAB) |
| C-38 a city-state's play | 1 | the purchase gate, the quest weights and eligibility, the walker's attacks, coastal and naval rates, the levy discounts' stacking (LAB); later-era starts (no start era) |
| C-41 Volcanic Soil | 1 | the rows' per-plot rates, a sea bonus resource on the ring, floods and storms under the same gate (LAB) |
| C-49 named storms | 1 | the step drops against an ocean mask, the resultants' bimodality (LAB) |
| C-60 the Free City's own play | 2 | the outward grant search, bankruptcy on the shortfall (BUILD); the pair tech by tech, the grants' fate on a fall, same-distance order (LAB) |
| C-74 the turn's one random event, the residue | 3 | N 251 with the first-occurrence boost, no-site draws empty, dormant volcanoes waking, the seven-plot drought start, floodable rivers on first reveal (BUILD); the once-per-map N, the warming lag, the drought sampler, `Spacing`, the fire clock, the meteor's grant, a blizzard on a wonder, a minor's pillaged buildings (LAB) |
| **C. Absent systems** | **15** | |
| **OPEN, TOTAL** | **25** | |

## The question ledger — owner asks

A question the SOURCE under-determines; neither engine ships a branch until
the owner rules or the live game answers. A ruled or measured line is
written into its entry and leaves this list. Numbers are stable (the lab
scenes cite them), so the gaps are closed asks.

| ask | entry | the question | where the answer comes from |
|---|---|---|---|
| 18 | C-2 | DON'T SETTLE NEAR ME's break: how near the promiser must found to break it, if ever. Which seat makes it is read (the founder, promising the seat it settled near); the plain settled-near grievance is 25 within 3 of a rival's border | LAB C-2-S1 (found at border 1 and 2 with the promise standing; read the grievance log in the same turn) |

## A. Engine vs engine

The digest is the only instrument for this class; a round that widens what
the gate reaches is worth more here than one that re-reads the exporter, and
a hunt catch opens an entry here only when it cannot close in the same
commit.

## B. Fidelity vs real Civ 6 — shipped mechanics with open tails

- **B-24r. GOVERNOR TAILS.** Weight 1.
  - LAB (no scene drafted yet): Industrialist and Renewable Subsidizer ship from their rows, with three readings — the plants' +2 Production stays in the governed city (not spread through an Oil or Nuclear plant's regional reach); a city several same-kind plants reach takes the best rate; carbon follows the Power generated (the pedia's "per Power generated"), so a governed plant emits more per resource.
  - BLOCKER C-38: Foreign Investor copies the strategics a minor accumulates and Affluence the luxuries it imports (`*_COPY_*_FOR_IMPORT`); a minor improves nothing here.
- **B-31r. TRADE-ROUTE TAILS.** Weight 1.
  - LAB B-31r-S1: the plunder payout's law. Ten plunders paid 50–110 in Gold alone — not flat, not an era table; two routes of equal origin yields paid 70 and 50; sea routes plunder too. The next read accounts the seed at the plunder (`seedBefore` / `seedAfter` in `b31r_run.py`) beside the route's own yields and length.
  - P8: the destination is one candidate row plus take/skip; the free-choice head is P8 work.
- **B-D. UNSOURCED DATA VALUES.** Weight 1.
  The online speed ships: `GAME_SPEED` is `GAMESPEED_ONLINE`'s `CostMultiplier` 50, and every cost the install scales (units and their steps, techs, civics, buildings, wonders, projects, districts, the Builder and Settler steps, the worship Faith price) and every `ScaleByGameSpeed` / `Scale` grant (Great People, wonder grants, tribal villages) goes through it, truncated as the lab's purchase record truncates odd costs (`scaleByGameSpeed` / `scale_by_game_speed`). The tile-purchase ring step and the space flight's light years are this engine's readings of the speed.
  A building's purchase price comes off its fractional scaled cost (`buyCost`; all 133 building rows of the lab's purchase record fit it, the units fit the truncated cost). Policy and government changes cost the policy-unlock gold outside the free window, the turn after a civic completes (`policyUnlockCost` / `_policy_unlock_cost`, `GovernmentState.civicTurn` / `civ_civic_turn`), paid by the applier once a turn: the `GameSpeed` Online row (50, dropping 5 a turn to 10) times k = 1.5 + techs/10 (the seat's researched techs), rounded to the nearest 5, halves up (`POLICY_UNLOCK_*`, `lab:` the two `policy_cost_bdprice*` records; the rounding's direction is this engine's reading).
  - LAB: confirm k by a paired arm (one tech granted, then a turn with a civic due) and read the rounding.
  - BUILD (B-D): emergencies take `GameSpeed_Durations` `ONLINE_HALF` (60 → 40, 30 → 20): the Nuclear emergency ran 40 turns online (B-86-S0).
  - LAB: which other durations the game scales online. `ONLINE_HALF` maps 30 → 20, 60 → 40, 10 → 8, 15 → 10, but no row a GS game loads names a scaling, so every other duration ships at its Standard value (deals, friendships, alliances, denunciations, promises, retribution and competitions 30; war and peace minimums 10; the formal war 5; the war buff 10; the research alliance 30; the congress 30 and a special session 15; spy missions; governor establishment; trade routes 20; the levy 30). A friendship's or a deal's expiry read online settles it.
  - LAB: amounts the text marks "(on Standard Speed)" with no data rule ship unscaled: the strategic stockpile's +10, units' resource costs, kill yields, the raid's +50, Wolin's 25%, Sweden's 50 Favor, conversion science, Great Person point costs, the pantheon's 25 Faith; and whether the game truncates tech, civic and grant amounts as it truncates purchase costs.
  A seat's government is its record's choice (`government`, validated against its civics; `GovernmentState.chosen` / `civ_gov_chosen`), the newest tier the default until it names one; the driver draws a persistent style per seat, so every tier-mate is adopted across games.
  - LAB: Anarchy. `GOVERNMENT_BASE_ANARCHY_TURNS` 2 against the pedia's "a minimum of three turns … longer the more you previously adopted it"; until it is read both engines refuse a return to a government the seat held before, so no seat enters Anarchy.
  - LAB B-D-S1: the per-city war-weariness split. The engines take `floor(WWP/400)` from every city (`warWearinessPenalty` / `_ww_penalty`); the game takes it from ONE city (two in 2 of 18 seats, `runs/ww_xsec_*`) — a founded loser is the seat's most populous founded city — pushing it 4–11 below its need, so the `LOSS_OVER_REQ_AMENITIES_{AT_WAR_CITY 3, FOUNDED_CITY 0, NONFOUNDED_CITY 1}` rows are neither caps nor addends. Next: a seat-level points reader (a method dump of `GetDiplomacy()` / `GetCulture()` / `GetStats()`), then each city's loss and population turn by turn as the loss grows and moves. The empire-wide rule ships until then.
- **B-82. THE TURN-LIMIT SCORE WINNER.** Weight 1.
  Civ 6's Score ships on both engines (`SCORING_LINE_ITEMS`, `scoreLines` / `scoreLeader`, `score_lines` / `leader()`): civics 3, techs 2, wonders 15, Great People 5 per person earned, the game's whole era score 1 per point (`eraScorePast` / `era_score_past` bank each era), Empire 5 per city + 2 per completed district (a wonder's own district included) + 1 per citizen + 1 per building its cities hold (`LINE_ITEM_ERA_BUILDINGS`, TieBreakerPriority 1030, the first tie line; the Palace and pillaged buildings count, a wonder is the Wonder line's alone), Religion 5 per belief; past `TURN_LIMIT` the highest total wins, ties broken by the higher `TieBreakerPriority` category, then the lower seat; only majors holding a city score. The digest compares the Score every turn; the `score_victory` lane reaches the winner.
  - LAB: the Palace's 2 (the engines count it 1); whether a pillaged building counts (the engines count it); Religion's `Converted` term (other players' cities following it, minors included, is the leading reading) and `TieBreakerPriority`'s direction — both by paired arms (a conversion, a tie).
- **B-86. THE EMERGENCY COMBAT TERMS' REACH.** Weight 1.
  The seat-pair combat terms ship in every air sub-combat on both engines — the anti-air burst, the interception and the strike: the emergencies' net difference on the plane's side, the Military alliance's +5 (Enkidu's with it) on each side for its own seat (`airPlaneTerms` / `_air_plane_terms`). REACHED by `tests/cpu/units/air-patrol.test.ts` and `tests/gpu/air_patrol_test.py`.
  - MEASURED (B-86-S0, lab 5, `lab4_t225`, saved `b86s0_nuclear_t244`): a nuclear strike raises the Nuclear emergency only at the next World Congress session (none for 15 turns; `EMERGENCY_NUCLEAR` in `GetEmergencyInfoTable` the turn after the t241 session: the striker the target, every other major a member, 40 turns left, the members' goal a capture). `GetEmergencyInfoTable` answers for every player.
  - LAB B-86-S1: the lost Nuclear emergency's `EFFECT_ADJUST_CITY_IDENTITY_PRESSURE` -1 (each member city pressing as one fewer citizen in `citizenPressure` / `_citizen_pressure_from`, against a city-level term); needs the target's cities held for the emergency's 40 turns, then `GetCityIdentityPressures` per source (a WON emergency's reads are in `runs/emergency_loyalty_20260926T091947Z.jsonl`).
  - MEASURED (B-86-S2 fallback, lab 5, `lab4_t225`: a level-1 Military alliance made by `SetHasAllied` + `SetPermanentAlliance(4, 3)`, then a war): the bomber's preview carries the alliance's "+5" once per sub-combat — the anti-air burst it takes fell from 80 to 66 (AA 95 against bomber 70, then 75), and four fired strikes matched it draw for draw; the interception fight takes it too. So the anti-air answer and the interception both satisfy `REQUIREMENT_COMBAT_UNIT_VS_UNIT`.
  - LAB: which side counts as the attacker in the burst (the emergency modifiers split attacker and defender); raising a Military or Nuclear emergency at all (S0).
- **B-89. FIRE ON A RELIGIOUS UNIT.** Weight 1.
  A shot — a ranged attack, a city's or an Encampment's strike, an air strike — never takes a civilian, ashore or afloat, on both engines (`shootable`; `_shot_class_at`, `_shot_embarked_plane`, `_nonbarb_unit_plane`); a melee order onto a tile whose only hostile occupant is a religious unit ashore is a step onto it that spends no attack (`meleeAttack`'s `MOVED_ONTO`, `_melee_coloc`), onto a Builder a capture; Condemn Heretic is one own-tile `CONDEMN` column appended last (`condemnHeretic`). REACHED by `tests/cpu/units/religious-target.test.ts` and `tests/gpu/religious_target_test.py`.
  - MEASURED (B-89-S1, lab 5, `lab4_t225`, `runs/religious_target_*`): a RANGED attack or a city's strike can never target a Missionary, an Apostle or a Builder, from seat 0, an AI or the barbarians (`CombatManager.CanAttackTarget` false 60/60 in both Lua states; the preview nil; fired requests do nothing). A MELEE order onto one is a move, not an attack: onto a religious unit it shares the tile and does nothing, onto a Builder it captures. Condemn Heretic is legal only on the heretic's OWN tile (false from every adjacent tile), kills it and ends the condemner's moves.
  - LAB: an air strike on a civilian (both engines refuse it, reading "all air attacks are ranged"; S1 fired ground ranged units and a city only) and a shot at a lone Support unit (both engines still take it).
- **B-91. A RELIGION'S BELIEFS.** Weight 1.
  A religion ends with one belief of each class on both engines (`BELIEF_CLASSES`, `adoptBeliefs` / `_apply_beliefs`): founding takes a Follower and one of Worship, Founder or Enhancer, enhancing takes the classes it lacks; the picks are the record's `beliefs` field and the driver's (`_decide_beliefs`). The nine Worship beliefs and their buildings ship (the Mosque's +1 spread charge, the Dar-e Mehr's pillage immunity, the Gurdwara's Housing, the Pagoda's Favor), built with Production or bought with Faith. REACHED by `tests/gpu/beliefs_test.py` and the `beliefs` lane.
  An Apostle's Evangelize Belief (`EVANGELIZE_BELIEF`) consumes it and earns the next belief (`beliefsEarned`, `RELIGION_INITIAL_BELIEFS` 2 at founding); the nine Founder and nine Enhancer beliefs ship (Lay Ministry, Sacred Places, Missionary Zeal, Monastic Isolation, Holy Waters built; Religious Colonization in the pool, inert); the Dar-e Mehr pays +1 Faith per game era since it was built (`City.buildingEras` / `city_bldg_era`). A seat holds one Great Prophet (`MaxPlayerInstances` 1); the Dar-e Mehr's era Faith counts toward religious healing; the GPU checks a unit's moves for every religious verb as TS does.
  - LAB: Religious Colonization's auto-spread (`EFFECT_ENABLE_RELIGION_AUTO_SPREAD`) names no pressure; Holy Waters reaches any seat's religious units as the install attaches it, against the text's "your"; the Founder beliefs' "cities following this Religion" is counted over the seat's own cities.
  - LAB: whose religion a worship building follows (`EnabledByReligion`): both engines use the owner seat's Worship belief; the city's majority religion is the other reading.
- **B-93. GREAT PEOPLE AS GATHERING STORM LAYERS THEM.** Weight 1.
  The census (`tools/civ6lab/gp_census.py`, every Great Person's layered rows against `GP_ABILITY`) lists what the catalog still misses; the admirals Rajendra Chola, Francis Drake and Ching Shih, Grace Hopper, Einstein's boost and Samori Touré ship as GS layers them, and a Great Person's unit grant takes the civilization's unique replacement (`UniqueOverride`).
  The census's admirals, generals, merchants, engineers and scientists ship as GS layers them (sites incl. City Center, Harbor, Spaceport and a Relic slot; `ActionRequiresNoMilitaryUnit`; unit grants placed map-wide by distance; `GP_BUILDING_YIELDS`; route, loyalty, title, extraction and happiness clauses), with readings: Rockefeller counts distinct improved strategic kinds, Ibn Khaldun's percent compounds on the tier factor, Hanno with nothing unlocked grants nothing.
  Galileo's, Darwin's and Janaki's own sites, the wonder engineers' incomplete-wonder tile, James of St George's missing-Castle gate, James Young's Oil reveal, Mary Leakey's Artifact tourism (her site needing an Artifact), Sun Tzu's Work of Writing and Eisenhower's military-only +5% ship.
  - LAB: whether Darwin's payout counts the natural-wonder plot he stands on (both engines count adjacent wonder plots only).
- **B-94. AN IMPROVEMENT'S AND A WONDER'S GROUND.** Weight 1.
  Every improvement takes only the features its `Improvement_ValidFeatures` rows list (`ImprovementDef.features`, `featureOk` / `_imp_feats_ok`; none listed means none, Volcanic Soil included), the Mountain Tunnel and Qhapaq Ñan too, so a natural wonder's mountain refuses them. Every land wonder reads its own rows on every plot it covers (`world/wonders.ts`, `wonderTileValid`): `Feature_ValidTerrains` (Kilimanjaro and Everest on Mountains), NoRiver, NoCoast and Coast, `Feature_AdjacentTerrains` / `NotAdjacentTerrains`, `Feature_AdjacentFeatures` / NoAdjacentFeatures, `Tiles`, and `Features.MinDistanceNW` 8; no latitude band for land wonders.
  The Lumber Mill takes Rainforest from Mercantilism (`Improvement_ValidFeatures` FEATURE_JUNGLE `PrereqCivic`; `ImprovementDef.featureCivics`, `_lumber_ground`); a volcano refuses every improvement that lists no volcano row, the Tunnel and Qhapaq Ñan included (`volcano_at`); the water wonders place by `MinDistanceLand` / `MaxDistanceLand` on salt Coast, the generator's three coast-expansion passes (`TerrainGenerator.lua`) laying the Coast they need; Yosemite, Eyjafjallajokull, Torres del Paine and the Cliffs of Dover take their `CustomPlacement` shapes.
  - BUILD: the generator runs `ResetTerrain` on every wonder plot (flattening hills and mountains) and `SetNaturalCliff` on custom-placed plots; neither engine does — this bears on Kilimanjaro's Mountain reading.
  - ASK: the water wonders' `NotNearFeatures` ICE radius ("Far based on map size", DLL-only); only the plot itself refuses Ice.
  - LAB: the shape of a multi-plot wonder with no `CustomPlacement` (Dead Sea, Pantanal, Everest, Eye of the Sahara, the water wonders — laid by the DLL's `SetFeatureType`); whether a water wonder may stand on a lake (a lake is TERRAIN_COAST in the install, a separate terrain here).
- **B-95. A CITY CENTRE'S STRENGTH.** Weight 1.
  MEASURED (`tools/civ6lab/city_defense_preview.lua`, the combat preview's own terms via `CombatManager.SimulateAttackVersus`, 1,774 centres over the 37 named saves, `runs/citydef_20260926T061603Z.jsonl`): the centre's `GetDefenseStrength` is the preview's base plus its DEFENSES lines on 1,752 of 1,774 (the rest carry a wounded garrison, the line fractional), one rule for majors, minors and Free Cities; a governor's +5 in one non-capital per major rides under the Palace line's label; the terrain lines (+3 hills, +5 river, -2 rough) are the fight's, not the centre's standing strength.
  Both engines ship it as ONE composer for every holder (`centreStrength` / `_centre_strength`): the holder's base (`holderStrength` / `_holder_strength`) + `Districts.CityStrengthModifier` over the city's complete, unpillaged districts (`DistrictDef.cityStrength`, exported per district: 2 each, 0 on the City Center, Aqueduct, Dam, Canal and Preserve; 704 of 706 census cities) + 3 per pre-modern walls tier + the Palace's 3 where the city holds it, a capital or any minor's city (`PALACE_ADJUST_GARRISON_STRENGTH`; `MODIFIER_PLAYER_CITIES_ADJUST_INNER_DEFENSE` carries no requirement set, the lab read it in the capital alone) + the garrison term, max(0, a land military unit of the holder on the centre's Combat - the base) x (1 - damage/200) (`garrisonCS` / `_garrison_cs`, `GARRISON_DAMAGE_SCALE`) + a minor's 1 per envoy it holds from every major together (`COMBAT_STRENGTH_FROM_ENVOYS`; `envoysReceived` / `_minor_envoys_received`, the raw store: a posted governor is no envoy received). The base is max(the Ancient `StartEras` row's `StartingMeleeStrengthMajor` 20 / `StartingMeleeStrengthMinor` 25, the strongest melee the holder has fielded) - 10 (`CITY_START_MELEE_MAJOR` / `_MINOR`, `CITY_BASE_MELEE_CUT`; `Seat.bestMeleeCS`, the GPU's `citystate_best_melee`, digest `minorBestMeleeCS`), and the Free City's flat 72. The garrison term makes the strength fractional: the GPU carries it as float64 through the defence, the city strike and the minors' strike and assault, a major's government and governor terms on top; the damage law quantises the difference to 0.1 and the digest compares hit points, not the strength. The Encampment reads it without the garrison. REACHED by `tests/cpu/units/city-combat.test.ts`, `tests/gpu/centre_strength_test.py` and every city fight in the serve gate.
  - MEASURED (C-38-S2 era half, 8 Small games, one per start era, `runs/city_defense_preview_c38s2_era*`, 112 minor and 48 major centres at turns 1–3): a minor's base is `StartEras.StartingMeleeStrengthMinor` − 10 at every era (25 → 15 Ancient, 40 → 30 Classical, … 70 → 60 Atomic), whatever melee it holds; a major's is max(`StartingMeleeStrengthMajor`, the melee its first city is granted) − 10 — an Ancient major's base is 10. The garrison line on 98 of 98 garrisoned start centres is max(0, garrison Combat − base) (a Warrior on 15 → +5, Pike and Shot on 40 → +15, a Spearman on 25 / 30 → none).
  - LAB: the base. One value per player per save (the Free Cities player per CITY: 45 and 55 at `lab4_t200`), most often the strongest melee its roster holds or can build minus 10 (88 of 247 player-saves over the 13 census saves) — the Civilopedia's "strongest melee unit built by your civilization, minus 10" — with residues (+7, +17, -10, -20) the held / producible reads cannot explain; a per-player "ever built" read (or its unit history) settles it, and whether `COMBAT_DISTRICT_STRENGTH_REDUCTION` 15 enters.
  - MEASURED (B-95 garrison, `tools/civ6lab/garrison_scale.py`, `runs/garrison_scale_20260926T081246Z.jsonl`, lab4_t150, player 1's capital, base 55 = its Line Infantry 65 - 10): the garrison line is NOT a flat +10 — it is (the garrison's Combat - the base) x (1 - damage/200), and nothing when the garrison is no stronger than the base: a Warrior (20) and a Musketman (55) add 0 at every damage; an Infantry (75) adds 20, 19, 17.5, 15, 12.5, 11 at damage 0, 10, 25, 50, 75, 90 (the centre's strength fractional, 92.02 … 84.02). The census's "+10 at full health" was the base's own minus 10: a garrison of the strongest melee adds exactly 10. The Civilopedia: "the strongest melee unit built by your civilization, minus 10, or … the Combat Strength of a garrisoned military unit". `COMBAT_GARRISON_MILITIA_MODIFIER` 10 is not this term.

## C. Absent systems — the blockers, and the gaps waiting on them

- **C-1. POWER.** Weight 1.
  The NUCLEAR ACCIDENT ships on both engines as measured (`nuclearAccident` / `_nuclear_accident`, `ACCIDENT_*` in `cpu/data/disasters.ts`): three rows of the turn's one event draw (C-74) at `OccurrencesPerGame` 1, one site per major city whose reactor age (`City.reactorAge` / `city_reactor_age`) has reached the row's `MinTurnAtRisk` 10 / 20 / 30; five draws, the Industrial Zone pillaged at DISTRICT_PILLAGED's 0 / 50 / 100, ONE citizen lost at POPULATION_LOSS's 0 / 0 / 80 (never the last), the units on the reactor's plot (the Industrial Zone) alone struck by `UNIT_DAMAGE_LAND` (0 / 50 / 100, `MinHP` 20 / `MaxHP` 50, one roll for the plot's land units) and `UNIT_KILLED_CIVILIAN` (0 / 50 / 100) through the shared `strikeUnits` / `_strike_units`, RADIATION_LEAKED's 2 / 10 / 20 turns of fallout on that plot alone; no ring improvement pillaged, no unit off the plot struck, the plant never pillaged, kept and ageing. REACHED by `tests/cpu/map/disasters.test.ts` and the `power` poke lane; the serve gate reaches it only past Nuclear Fission.
  A Free City's reactor keeps ageing and is a site (`ageReactors` / `_age_reactors` from `freeCitiesPhase` / `_free_cities_phase`); a flip carries the reactor's age.
  - LAB C-1: the building rows, which neither engine ships. Over 150 forced accidents (`runs/reactor_reactor_base_*`) the Power Plant was never pillaged and CATASTROPHIC's `BUILDING_DESTROYED` pillaged as the others do; the Workshop / Factory were pillaged MINOR 5 / 3 of 50 (never both), MAJOR 36 / 18, CATASTROPHIC 30 / 12 (the Factory only with the Workshop). Open: one building per event or per building; why MAJOR's 100 leaves 5 of 50 events untouched; the CATASTROPHIC land band reaching 93 (two rows landing, or the fallout); the MINOR record's reactor-plot hits (5 land units killed, 5 damaged at 50, no unit row at MINOR) and units vanishing unharmed at distance 1–3 (9 / 1 / 5 by severity).
  A city-state's grid is powered by renewables alone (`minorPower`, `_minor_power`): it holds no stockpile and builds no plant, so no reactor ages there.
- **C-2. DIPLOMATIC AGREEMENTS.** Weight 2.
  The promises ship on both engines: the four `DiplomaticActions_XP2` rows (`cpu/data/promises.ts`, `eras.promises`), a ledger per ordered pair (`promiseWith` / `seat_promise`, `promiseBrokenWith` / `seat_promise_broken`), the ask and the answer settled in one turn (`settlePromises` / `_settle_promises`), the incursion (`promiseIncursion` / `_promise_incursion`) called from the offensive spy mission, the city that comes to follow the promiser's religion and the dig worked on the asker's ground, and the War of Retribution's `brokenPromise` reading the window. The plain "settled too near" grievance ships (`grievanceSettledNear` / `_grievance_settled_near`, from `foundCityAt` / `_found_city_at`): every other major owning a plot within 3 of the new centre gains it against the founder. REACHED by `tests/cpu/seats/promises.test.ts`, `grievances.test.ts` and the `promises` poke lane; the serve gate through the driver's `_promise_turn`.
  - BUILD (C-2, both engines): the settle promise as measured (`runs/promise_*`, the grievance log read in the turn of the act): founding within 3 of a rival's BORDER costs nothing until the rival asks, at that turn's end; a refusal costs 25 (`GrievancesForRefusal`) and each later founding within 3 another 25; a kept promise breaks on a founding at border distance 1–3 for 100, each further founding within 3 costing 25 × `GRIEVANCE_MULTIPLIER_FOR_BROKEN_PROMISE` 200% (`promiseIncursion`); border 4 and beyond costs nothing. Delete `grievanceSettledNear` / `_grievance_settled_near`: the standalone near-settle grievance does not exist.
  - Model lines, identical on both engines and kept as notes: a luxury has no lump to trade (the install trades ACCESS, never an amount); the intel bonus is unit-against-unit only; one running deal per ordered pair, `DEAL_ITEMS` a side, an offer standing two turns; a war does not end a standing deal or promise; the Third Party War's "another player" is read as an ALLY; a refused ask stands the pedia's 30 turns; the refused asker's favor comes back at the refusal, where the pedia says "refunded for the next session"; an offensive mission is an incursion whatever its outcome; an ask is answered in the turn it is made; an ask the asker cannot make is dropped, neither paid nor refused.
- **C-16. THE SPY'S SECOND HALF.** Weight 1.
  - The escape ships on both engines as measured (`spyEscape` / `_spy_escape`; lab 4, `spy_loop.py`, 129 escapes): the police guess one offered route uniformly, and the spy gets away when the 3d6 lands at or under 10 + its level (-4 on a right guess). One-route cities let 10 of 54 away (14.4 predicted); two-route cities 41 of 63 (32.9 predicted, about +2σ); a lost escape ended in the cell 34 times in 117.
  A counterspy guards its district and every adjacent one (`LOC_ESPIONAGECHOOSER_COUNTERSPY`; read across cities), and the highest-level post pursues (`counterspiesGuarding` / `_counterspies_guarding`).
  - LAB C-16-S1: `ESPIONAGE_ESCAPE_COUNTERSPY_LEVEL_MODIFIER` -1 per level is applied as written, unmeasured: all 234 prompts named the police, and no save holds an AI counterspy. BLOCKED on posting one: `SetReturnAsPlayer` makes another seat local but its commands are dropped, and a counterspy requested on an AI's spy never starts — the scene needs a game with two human seats (hotseat or a two-human config).
- **C-20. THE ROUTE'S TRANSPORTATION EFFICIENCY.** Weight 1.
  Routes walk through Mountain Tunnels and Qhapaq Ñan, one portal network per range (`portalExit`, `tradeWalkable` / `tradeWalkStep`, `_trade_walk_step`). Every leg of every seat pays the measured path term (`routePathGold` / `_route_path_gold`, from `cityTradeYields` / `_seat_route_income` / `_minor_route_income`; `ROUTE_PATH_*` from `TRADE_ROUTE_TRANSPORTATION_EFFICIENCY_*`, the 256ths `lab:`) over the Trader's own descent (`tradeWalkPath`): D is the destination rows' Gold (a city-state's flat Gold under Sovereignty), T the crossed foreign cities short of the destination holding the seat's post (the destination's is `routePostGold`); a pair no descent reaches earns none. A chain city and another civilization's post pay nothing of their own (`routeChainGold` keeps Jakarta and Rome). The route chooser's scores leave the path term out.
  - LAB: the Trader walks a greedy step toward its destination (`tradeWalkStep`) and a route's range is a hex distance, where the game runs its pathfinder over both.
- **C-26. CIVILIZATION ABILITIES — THE RESIDUE.** Weight 1.
  The census is `docs/roster_ledger.json`, read as `docs/ROSTER.md` says (354 of 356 rows `shipped`, two `AI:`). A city's strike composes the struck unit with the roster's rows (`cityStrikeDefenderCS`, `_seat_city_strike`); no roster row carries an attacking or defending requirement, so one composer serves both sides. Qhapaq Ñan ships (`MOUNTAIN_ROAD`), and a wonder takes floodplains as GS allows (`Features_XP2.ValidWonderPlacement`). A Trader goes to sea, Ocean included, once its seat holds Celestial Navigation (`tradeWaterLevel` / `_trade_water_level`: no Cartography gate, no Norway branch).
  - The city strike's defender still omits `defenderCS`'s other unit-vs-unit terms (`chassisAbilityCS`, `barbarianCombatCS`, `visibilityCS`, `allianceWarCS`, `eraMatchupCS`); audit which apply to a struck unit before building.
- **C-34. AIR COMBAT'S SECOND HALF.** Weight 1.
  Patrol, Priority Target and the sortie's order ship on both engines. A fighter deploys over a hex within its Moves of its base (`deployAir` / the `DEPLOY_k` columns, which offer the seat's own district tiles; Return to Base costs no Moves) and holds it until another air order; it intercepts a sortie aimed within 1 of that hex: the patrol on the struck tile first, else the strongest Combat among the nearest, in a TWO-SIDED fight at Combat (the interceptor's damage drawn first, an interceptor at 0 HP gone), each other covering patrol backing it +5 x hp/100 (`INTERCEPT_RANGE`, `INTERCEPT_SUPPORT_CS`; `interceptorAgainst` / `_interceptor_scan`, `interceptFight` / `_intercept_fight`); an intercepted fighter aborts, a bomber flies on; stationed aircraft never intercept, and a patrol heals only with Ground Crews. Priority Target strikes the tile's Support-class unit past its combat unit for a flat 65, no draw and no answers (`priorityTargets` / `_priority_targets`, `PRIORITY_TARGET_DAMAGE`). A sortie meets interception, then the anti-air cover, then strikes only if it survives, the bomb wrecking at 50 HP or more after the answers; a bomber striking a city meets both (`airAnswers` / `_air_answers`). REACHED by `tests/cpu/units/air-patrol.test.ts` and the `air_patrol` lane (no seed trains aircraft).
  - MEASURED (C-34-S1, lab 5, `runs/air_patrol_*`, `air_strike_*`, `air_bomb50_*`): a patrol intercepts within radius 1 (distance 0 and 1, not 2); a stationed aircraft never intercepts. The interception is a TWO-SIDED combat at the ordinary damage law — the interceptor at its Combat (a Biplane 80, not its Ranged 75) against the bomber's Combat (85, not its Bombard), both damaged (Δ3: 34 and 27; two patrols 41 / 22; three 50 / 18; all 30·1.04^Δ). Support is +5·hp/100 per other covering patrol (+10 at full health, +7.5 with one at 50 HP). The interceptor is the patrol on the struck tile first, even wounded or weaker; among equals the stronger (the pedia's "highest strength aircraft is chosen" dies on 2 rows). In a real strike the AA gun fires first on draw 1 and the strike runs on draw 2 at the bomber's health after the burst; a lone AA gun defends at AntiAir 90. PRIORITY TARGET fired deals a flat 65 to the AA gun, no draw, no damage back (`COMBAT_MIN_CIVILIAN_DAMAGE_PERCENT` 65; its preview shows an ordinary combat). The bomb pillages when the bomber holds 50 HP or more after its answers (51 and 50 pillaged, 49 / 48 / 46 not).
  - LAB: XP, war weariness and the draw order of an interception (no real interception could be fired: the AI bomber cannot be ordered and AI jets never patrol); whether a Priority Target under a patrol is intercepted (the fired strikes met none, and no covering gun fired).
- **C-38. A CITY-STATE'S PLAY.** Weight 1.
  OWNER 2026-09-23: a city-state is no agent, so its behaviour IS the environment an agent trains against, and a gap an agent can exploit there is a sim-to-real gap. Every observable city-state behaviour is therefore an engine rule: built from the install where it publishes rows, measured in the lab where it does not, and randomised per episode where neither settles it, so no policy leans on one guessed value. Its food and culture already ride the majors' composers.
  - SHIPPED, both engines: the Ancient starting army (two Warriors from `loadWorld`, riding the GPU's majors' pool under 100 + id), which holds, digs in, heals, defends and is removed with the minor's conquest; the walled minor's city strike (`cityStrikes` from `minorPhase`, `_city_strikes` from `_city_state_phase`); the production rows (`MINOR_CIV_PRODUCTION_PENALTY` -50% and the toward-rows, measured to MULTIPLY: x1 plain, x3 walls / Builder / military, x6 the type district; `minorProduction`, `toward`). REACHED by `minor-army.test.ts`, `minor-strike.test.ts`, `minor-record.test.ts`, the `minor_record` and `minor_builds` lanes and the serve gate.
  - SHIPPED, both engines: ITS BUILD ORDER AND TRAINING, fitted to the census (`tools/civ6lab/minor_build_fit.py`): `MINOR_BUILD_ROWS` (Builder while none stands, Monument, melee while the army is under 3, Granary, a ranged unit while none stands, the type's district, tier-1 building, Water Mill, Harbor, walls, Lighthouse, tier-2 building, the later walls, Military Academy, Shipyard, Neighborhood, Sewer, Seaport, and an army row under a per-episode cap), each drawn row's timing drawn once per episode (`minorPlan` / `_minor_plan`); the army row trains the class the army holds fewest of against the census weights; the Builder and military production rows now reach their units. A minor's Builder improves a random valid (plot, improvement) pair on its land within 2 of the centre at the measured 236‰ a turn (`minorBuilders` / `_minor_builders`). The quest's camp radius is 5; the GPU's walls tier reads the minor's Urban Defenses as TS does.
  It runs TRADE ROUTES from its city (the Trader row at the census's timing; the destination the engine's own scorer, minors first, then majors; `minorTrade` / the GPU's minor trade), builds the district projects, the wall repair, worship buildings, the Flood Barrier and a Food Market, and buys naval units at the census's coastal rate (`MINOR_NAVAL_BUY_BP`, a lab estimate). A district on an improved plot removes the improvement, feature and bonus resource for every seat (`paveGround` / `_pave_plot`). The levy hands the minor's own military to the suzerain for `LEVY_TURNS` 30 at 25% of their purchase cost (`LEVY_MILITARY_*`), and returns them.
  The Foreign Ministry's 50% levy discount, the Logistics project's full power while it runs, the district projects' per-row yield conversions (the Commercial Hub's 30%, the Training project's 15% Gold) and the Shopping Mall (exclusive with the Food Market) ship.
  - LAB: whether the levy discounts add (Sumeria's and the Foreign Ministry's 50% each, capped at 100 as built) or stack another way.
  - SHIPPED, both engines: ITS PURSE AND ITS WALK (`tools/civ6lab/minor_play_census.py`): a Builder bought when none stands at a per-episode rate (about 8% a turn), a military unit bought above a 95-gold floor at a rate falling with army size (0 at 8+, ×3 within 3 turns of a loss), one upgrade per completed tech or civic at 5 gold (`UPGRADE_BASE_COST` × speed), a Warrior Monk for faith where the belief allows; the minor pays unit upkeep and its treasury floors at 0; it ignores strategic requirements (`CivilizationLevels`); its land units walk the measured step and distance tables for peace, war and damage (`cpu/core/walker.ts`, `_walk_units`).
  A city-state's city holds the Palace (`BUILDING_PALACE` `Capital`, each minor a `CapitalName`): the census's +5 Gold a turn from founding, with the Palace's Production, Science, Culture, Housing and Amenities.
  - MEASURED (C-38-S1, one Standard game, 12 majors / 12 minors / 250 turns, `runs/c38s1_watch_c38s1_ext1_*`): 230 quest offers — ZONE_DISTRICT 44, SEND_TRADE_ROUTE 38, TRIGGER_CIVIC_BOOST 35, RECRUIT_GREAT_PERSON 33, TRAIN_UNIT_TYPE 33, TRIGGER_TECH_BOOST 32, CONVERT_CAPITAL 13 (from world era 2), CLEAR_BARBARIAN_CAMP 2; exactly one standing quest per (major, minor) pair on all 8,083 pair-turns; the reward 1 envoy (230/230); a quest stands a median 26 turns (1–99+, no fixed expiry); the gap to the next offer on a pair has median 19 and 39 of 119 gaps are under 12, so `QUEST_COOLDOWN` 12 dies as a floor. A minor's military steps 0: 26%, 1: 48%, 2: 21%, 3+: 4% a turn (5,623 unit-turns) and stands d0 12%, d1 26%, d2 24%, d3 13%, d4 11%, d5 11%, 6+ 4% from its centre; its Builders stay put 78% of turns and within 2 of the centre 99.7%; improvements on its plots: Farm 50, Mine 27, Nazca 6, Fishing Boats 5, Pasture 4, Monastery 3, Lumber Mill 2, one each of Camp, Quarry, Plantation, Ski Resort, Geothermal. Its techs average 4.4 at t25, 9.5 at t50, 26.3 at t100, 37.3 at t150, 46.5 at t200, 70.0 at t250.
  - MEASURED (C-38-S2 era half): a minor starts with its era's `BonusMinorStartingUnits` rows below Emperor exactly (the Pike and Shot swap is `Expansion1_Eras.xml`'s), plus the Settler and a Trader at founding; starting buildings and populations follow the StartEras / StartingBuildings rows (population 4 from Classical; at Atomic no walls were granted).
  - LAB: two more extended games for the quest weights and per-major eligibility; the walker's attacks (none recorded).
  - The centre strength's base is B-95's LAB tail; its terms ship for every holder.
  - BLOCKER, no start era: `BonusMinorStartingUnits`' later-era rows (Classical to Information, GS swapping Pikeman for Pike and Shot) wait on a later-era start existing on either engine.
  - LAB: the naval-buy rate (31 buys over 4,979 coastal-evidence minor turns) and the Research Lab, Stock Exchange and Broadcast Center rows the census never saw built.
  - LAB C-38-S3: which minor-specific condition gates a buy. The Builder buy rises with the bank (`runs/c38s3_*`: 300 → 16 of 30 within 5 turns, 160 → 6 of 20, 110 → 2 of 20, never below the price) and the same minors buy in every repeat; army size alone never triggered a military buy (0 in 600 minor-turns at bank 300, army 0–10), against the shipped army-size rate.
- **C-41. VOLCANIC SOIL.** Weight 1.
  Both engines paint the RADIUS-1 RING as measured (`erupt` / `_erupt`): four draws per eligible plot in ring order (`soilPaintable` / `_soil_paintable` — land, no Mountain, no district, city centre or wonder, bare or Woods / Rainforest / Marsh, never an Oasis), Volcanic Soil at the severity's YIELD_FOOD row (35 / 50 / 75), replacing the Woods, Rainforest or Marsh and a Lumber Mill; a plot the draw paints gains +1 Production, Science and Culture silt (`Tile.fertilityProd` / `fertilitySci` / `fertilityCul`, `fertility_prod` / `fertility_sci` / `fertility_cul`, capped at 3, paid by `tileYields` and `_silt_y`) at the row's own `RandomEvent_Yields` chances (`ERUPTION_PROD_P` 15 / 25 / 35 on the volcano's rows, `ERUPTION_SCI_P` 0 / 10 / 15, `ERUPTION_CUL_P` Vesuvius's 50 alone); then they scorch (pillage) and fertilize the ring. The radius is sourced: `RealismSettings.ExtraRange` is false at MODERATE. REACHED by `tests/cpu/map/feature-add.test.ts`, `disasters.test.ts` and the `feature_add` lane.
  The eruption's `RandomEvent_Damages` rows ship (`ERUPTION_*` over the five eruption rows, `eruptTile` / `_erupt_tile`): pillage, `IMPROVEMENT_DESTROYED`, `DISTRICT_PILLAGED`, `BUILDING_PILLAGED`, the city centre, `UNIT_DAMAGE_LAND` with its band, `UNIT_KILLED_CIVILIAN` and `POPULATION_LOSS` (only where the plot has an owning city), on an OWNED ring plot only — the plot's owner decides, not the improvement's — with six draws per plot whoever owns it. Every BONUS resource on a land plot of the ring is lost whoever owns it (`_drop_resource`, the harvest's resource-free copy); strategic and luxury resources stay.
  - LAB: the production and science rows' per-plot rates (fit from the two records; the engines roll each row's `Percentage` on every painted plot); whether a SEA bonus resource on the ring is lost too (both records read land plots; the engines take a land plot's alone); whether floods and storms take the same owned-plot gate.
- **C-49. NAMED STORMS.** Weight 1.
  `stormWalk` / `_storm_walk` ship the measured model: eight unit steps on the movement turn from the `PrevailingWinds` band at the centre's current latitude, eight more with no footprint on dissipation.
  - LAB: an ocean mask per save to simulate the dropped steps and explain the resultants' 7–8 / 10–13 bimodality (an open plane predicts median 10.5); C-49-S1's batch hurricanes only if the mask leaves the walk unpinned.
- **C-60. THE FREE CITY'S OWN PLAY.** Weight 2.
  The seat is in on both engines (revolt, race, join, Eleanor's skip, open to attack, the religion walk). Its city stands on a flat 72 base (`FREE_CITY_DEFENSE`; `holderStrength` / `_holder_strength`, measured once, Medieval, no walls, no garrison) with every holder's terms on top (`centreStrength`); a walled Free City fires the ordinary strikes; the centre heals at the ordinary rate and its loyalty restarts at 100. REACHED by `tests/cpu/city/free-city.test.ts` and the `free_city` lane.
  BANKRUPTCY ships for every seat, the Free Cities and city-states included (`GOLD_NEGATIVE_BALANCE_*`, `bankruptAmenities` / `bankruptDisbands`, `_bankruptcy_count`): each city loses 1 amenity once the treasury drops below 0 and one more per further 10; at -10 one unit disbands and one more per further 10, the priciest first, never a free unit; the treasury may go negative. The Free Cities play ships too: the revolt grants two of the FORMER OWNER's best melee (`freeCityPairType` / `_free_pair_type`: the strongest its techs and civics unlock, no strategic resource asked), a unit every 5 turns while the city stays Free drawn over the census's class mix (`FREE_CITY_GRANT_WEIGHTS`, `tools/civ6lab/free_city_census.py`), its chassis the world era's (`eraUnitOfClass` walks the class's upgrade chain by unlock era), a treasury (`freeSeat.treasury` / `free_treasury`) banking its cities' gold and paying its units' upkeep, and on a join the city's own grants (`Unit.freeCity` / `unit_free_city`) are removed.
  It trains and builds from `FREE_CITY_BUILD_ROWS` (a ranged unit, Monument, Granary, walls, the repair project, a siege unit, district buildings), its research read as every tech and civic of the world era, and its units walk the minors' body with its own tables. Its treasury floors at 0 as the minors' does, and bankruptcy reaches the majors alone.
  - LAB C-60-S2 also: its own techs (Slingers at turns 90–114 where the world era's research gives Archers); its districts (4 of 28 records), the Flood Barrier and district projects are not hosted.
  - The centre strength's base is B-95's LAB tail (a Free City's base is set per city).
  - LAB C-60-S2: the flip pair tech by tech (a tech-list read); removed vs transferred on the new owner's side; the grant's type draw, its income, its walker's targets.
  - BUILD (C-60): a grant with every nearby land plot held lands on the NEAREST FREE LAND PLOT outward, never skipped and never on the centre (`runs/c60s3_r*`, 4 of 4) — `grantFreeCityUnit` / `_grant_free_unit` search outward.
  - LAB: the order among free plots at one distance; whether support units spawn on the centre.
  - BUILD (C-60 / every seat): bankruptcy on the turn's shortfall S (maintenance minus whole-gold yield), not the balance (`runs/bankrupt_*`): each city loses 1 + floor(S/10) amenities while S > 0, flat however long it lasts (`bankruptAmenities` / `_bankrupt_amenities`); exactly ONE unit disbands a turn while S ≥ 10, the first with upkeep in the seat's roster order, not the priciest (`bankruptDisbands`); the treasury clamps at 0.
- **C-74. THE TURN'S ONE RANDOM EVENT, THE RESIDUE.** Weight 3.
  Both engines fire at most ONE random event a turn (`randomEvent` / `_random_event`, from `disasterPhase` / `_disaster_phase`): one draw over the eligible (row, site) pairs in the install's `RandomEvents` order, each pair firing with the ABSOLUTE chance of its row's `OccurrencesPerGame` at MODERATE (`eventRows` / `_event_rows`, warmed) over the row's normaliser — `EVENT_NORM_PER_SITE` 240 for a row counted per site (floods, eruptions, accidents), `EVENT_NORM_PER_MAP` 250 for a row counted once per map (storms, droughts, fires, the meteor), the Standard readings (`lab:` C-74-S1) at every map size — and the turn EMPTY with the rest; chances summing past 1 are scaled to sum to 1 (`x = r · max(1, Σ)`). The sites: the three floods once per river carrying Floodplains (`floodSites`, shipped static as `floodStarts`), Kilimanjaro's two eruptions once while it stands, the three eruptions once per ACTIVE volcano, the eight storms once each while their start terrain exists, the three nuclear accidents once per reactor past `MinTurnAtRisk` (C-1), and the two droughts once while a city holds a start plot in reach. The drawn row IS the severity; a storm's centre is a second draw, a drought's three (`droughtStart` / `_drought_start`). The spy's breached Dam draws its flood's severity by the same weights (`floodSeverity` / `_flood_severity_draw`). REACHED by `tests/cpu/map/disasters.test.ts` and the `storms`, `feature_add`, `flood_severity`, `pack_events` and `power` lanes.
  A volcano is ACTIVE or not for the whole game: `deriveVolcanoActivity` draws each volcano once when the game is made from the map (`createGameFromMap`, one draw per volcano in tile order on a stream derived from the game's `rngInit`, not the turn stream) at `RealismSettings.PercentVolcanoesActive` 70 (MODERATE), and the exporter ships the active ones (`activeVolcanoes` → `volcano_tile`). The re-draw in play is not shipped: its clock is the LAB line below.
  A flood STARTS on its river's first floodplain plot, read in this map's river model (which keeps no flow direction) as the river's UPSTREAM-MOST Floodplains plot: the one farthest, in steps across river edges, from the river's plots touching the map's water as made (its mouth), ties to the lowest index; a river touching no water, and a lone Floodplains plot, start on the lowest-index one (`floodStart`). The flood still reaches the whole river from any start.
  A drought is ANCHORED on a city (`droughtSites` / `_drought_sites`): the city is drawn uniformly over every city — a major's, a Free City's, a city-state's — holding a featureless Plains / Grassland plot within 3 of its centre, the distance by the measured mix 0: 11, 1: 41, 2: 46, 3: 26 over the distances where that city holds one (`DROUGHT_DISTANCE_WEIGHTS`, `lab:` C-74-S1), the plot uniformly over its plots at that distance.
  The drought, the start turn and the warmed weights ship: a drought starts on featureless Plains / Grassland within 3 of a city centre, pillages the listed improvements (`DROUGHT_IMPROVEMENTS`, destroyed at EXTREME's 30%), bans rebuilding and repairing them while it lasts, and spares the food of a city holding an unpillaged Aqueduct (or Bath), Dam or Stepwell (`droughtShielded` / `_drought_shield`); no event fires before `RANDOM_EVENT_START_TURN` 2; each row weighs `OccurrencesPerGame` × (1 + `ChanceIncreasePerDegree`/100 × ΔT), ΔT the world's carbon over `CO2For1DegreeTempRise`, continuous (`warmedWeight` / `_warmed`).
  The Gathering Storm pack rows ship (`GranColombia_Maya_Expansion2.xml`, `VikingsLandmarks_Expansion2.xml`, criteria `RULESET_EXPANSION_2`), in the live game's `RandomEvents` order: METEOR_SHOWER (one site on unowned, unimproved Plains / Grassland / Snow / Desert; its Meteor Site is claimed like a village for the next Heavy Cavalry line unit, which pays no resource upkeep), FOREST_FIRE and JUNGLE_FIRE (burning, burnt and regrowing Woods and Rainforest, 50% spread, the damage band, fertility), Eyjafjallajokull's two eruptions and VESUVIUS_MEGACOLOSSAL (their ring every plot touching the wonder). A decision-free TS / GPU run of the disaster phase matched turn for turn on seven seeds.
  Eyjafjallajokull and Vesuvius stand in the natural-wonder roster (`world/features.ts`, `world/wonders.ts`); a wonder's terrain list and inland rule hold for every plot it covers. In the locked worlds Vesuvius stands on five seeds (9027, 9209, 9235, 9287, 9300) and Eyjafjallajokull on none — the placement rules put it on about 5% of maps.
  Kilimanjaro stands on a lone Mountain as its rows say, and every wonder's placement reads its own rows (B-94).
  - LAB: whether a Blizzard starts on a Tundra wonder plot (`stormFamilyAt` reads terrain alone).
  - LAB: a disaster's BUILDING_PILLAGED on a city-state's district, and how the minor repairs it. Both engines pillage the buildings of a major's and a Free City's district (`pillageTileBuildings` / `_pillage_tile_buildings`); a minor's buildings stand, since nothing in its play repairs one.
  - LAB: the fire clock (regrowth at turn 6 by the Yields row, 8 by the notification; whether spread plots share the event's clock; whether a Forest Fire spreads into Rainforest); the meteor's grant ("a more powerful unit" of the class) and who may claim a site; the fire features are `ValidForReplacement` yet an eruption does not paint them (nor an Oasis; a Marsh it paints, C-41).
  - BUILD (C-74, correcting what shipped): N = 251 for the per-site rows with the 30% first-occurrence boost per (row, site) (`GlobalParameters` `FIRST_TIME_OCCURRENCE_BOOST`); a drawn row with no valid site is an empty turn (drought, tornado); volcanoes DORMANT at start with a per-turn wake draw (~0.6%, `lab:`) in place of the load-time 70% draw; the drought start's seven-plot condition (the plot and its ring all featureless Plains / Grassland) within 3 of the city; floodable rivers gated on a major's first reveal of a river plot.
  - LAB: the once-per-map rows' N and its map-size law (Duel cold reads 5 / 2 / 1% put it in (933, 1120]; storm's warm reads 1028.5–1030, drought and fire ~1003–1010); the warming shape and its lag (the percent holds its cold value while T < ~0.4–0.5); the drought's placement sampler (the seven-plot condition is necessary, not sufficient: 5 or more qualifying plots placed 11 of 22); what wakes a volcano; the two rivers that turned floodable 54–64 turns after the first reveal; `Spacing` 15 (neither a per-row or per-family turn gap nor a distance between live storms or droughts); a recapture game for a falling T.

## Harness — not weighted

- **H-1. THE AUTOPLAY DIFFERENTIAL HARNESS.** A Civ 6 game autoplayed to turn 250 becomes thousands of per-turn, per-subsystem checks against both engines. Not a replay: the game keeps no input log and our roster is a subset, so the final state never matches and need not. Built when the owner says so; output under `tools/civ6lab/`.
  - BUILD: a full-state dump every turn over the tuner — map (terrain, features, rivers, resources, ownership), cities (population, districts, buildings, worked plots, queue, pools), units, techs, civics, policies, religion, loyalty, and the GameCore seed at turn start — from the lab's probes (`state_snap.lua`, the `save_sweep.py` readers).
  - BUILD: the importer, the largest piece — the game's turn-t state into `GameState` (`cpu/core`) and the GPU fixture form, reporting the roster gaps per game.
  - BUILD: the checks at t+1 that read state alone: each city's yields, growth, housing, amenities, loyalty, religious pressure, border growth, purchase prices, upkeep, tourism, era score and city defence.
  - BUILD: actions reconstructed from state diffs (a unit moved, a district appeared, a tech completed) feed the action-dependent checks; an ambiguous diff skips its check, never guesses.
  - LAB: the draws each random subsystem takes per event (combat, interception, goody huts, random events), so the turn's seed turns them into exact predictions; whether the game writes a per-draw random log (the `Logs` folder, the config flags).
  - BUILD: a report per game (every failed check with the state it failed on), then a loop over seeds and settings on the fleet (H-2).
- **H-2. THE OBSERVER FLEET.** `tools/civ6lab/fleet.py` runs N instances of back-to-back games: every instance launched at once, a game loop per host under `watch.py`, the stop file, a free-memory floor, a log per host. Five instances on the lab profile (~5.7 GB each) is the measured optimum, ~15 Standard games an hour.
  - BUILD: `game.py new` with `all_ai` leaves slot 0 human, so an observer game never advances; every seat must be an AI.
  - BUILD: a fresh map and game seed per game, recorded; a manifest JSONL per run (game, host, config, seeds, wall times, turns reached, crashes and reconnects).
  - BUILD: crash recovery (a dead instance relaunched, its host loop continuing); an instance past a memory cap closed and relaunched between games; a wall-clock budget as a stop condition.
  - BUILD: one command that summarises a run (`cs_analyze` over its logs, throughput).
- **H-3. CIVILIZATION VI'S OWN MAP GENERATION.** An optional generator: the game's map settings (script, size, the map and game seeds, sea level, temperature, rainfall, world age, resources, start positions, majors and city-states) give the game's own world plot for plot — terrain, hills, mountains, cliffs, rivers, floodplains, lakes, features, natural wonders, resources and every start plot — as a `world@1` file both engines load (`loadWorld`), for training and evaluation. `world/mapgen.ts` only imitates Continents' look and is deleted once this matches.
  - BUILD: the install's map scripts run unmodified in an embedded Lua VM (fengari), read at run time in modinfo order (`Base/Assets/Maps`, `Maps/Utility`, `DLC/Expansion2/Maps` over them), with the native API they call rebuilt: `TerrainBuilder.GetRandomNumber` (212 call sites), `Fractal.Create` / `CreateRifts`, `TerrainBuilder.StampContinents`, `AreaBuilder.Recalculate`, `TerrainBuilder.AnalyzeChokepoints`, `GenerateFloodplains`, `AddIce`, `GetFractalFlags`, `CanHaveFeature` / `ResourceBuilder.CanHaveResource` (from the install's rows), `StartPositioner.GetPlotFertility` / `GetMajorCivStartInfo`, `Map.FindSecondContinent`, the plot order and the grid's wrap. B-94's `ResetTerrain`, natural cliffs and multi-plot wonder shapes come with it.
  - LAB: the map's random generator — `TerrainBuilder.GetRandomNumber` sequences for known seeds in a map-script context, fitting the algorithm and its seeding from `MapConfiguration` `RANDOM_SEED`; whether the game seed enters map generation (start plots, goody huts) and how the map hands over to the game's own random stream.
  - LAB: the native builders' outputs for known seeds (the fractal grids first), then the acceptance sweep on the fleet: for many (script, size, settings, seeds) the game's map dumped after generation against ours plot for plot, naming the first stage that diverges (terrain, rivers, features, resources, wonders, starts). Done at 100% on every Gathering Storm map script.
