# Engine audit — open work

THIS FILE IS A LIST OF OPEN WORK. A resolved entry is DELETED, not
annotated; what was fixed, when and why is the git log's job (`git log -S
<id>` finds an entry's history). One weight and one OPEN list per entry;
every bullet is one of four things and says which:

- **BUILD** — buildable now from a sourced rule; names the symbols.
- **BLOCKER** — waits on another entry; names it.
- **ASK** — the source under-determines it; the owner rules (the ledger below).
- **LAB** — the live game answers it (`tools/civ6lab/SESSION5.md`, scene named).
  A rule the install carries no number for is inferred from what the live game
  does: a scene that varies its inputs and fits the observed outcome.

**RULES (owner):**
- Anchor code BY SYMBOL, never by line number.
- VERIFY-BEFORE-IMPLEMENT against a real Civ 6 source: the owner's install
  (XML over civilopedia; Base <- Exp1 <- Exp2, take the last) first, the
  live game second, forums last. An unsourced magnitude is an ASK, never an
  invention.
- A gap deferred on an unbuilt mechanic is TWO open items: the mechanic and
  the gap naming it. "Recorded" and "descoped" are deferrals, never closures.
- Every new mechanic records which lane REACHES it; a green gate over an
  unreached mechanic proves nothing.
- When an entry closes, delete its row and its entry in the SAME commit.

**State:** P8 training PARKED until this file is empty.

## Open weight

Hand-weighted 1–8 by the size of what is LEFT to build. One row per entry,
no row without an entry. The rows are the source of truth; the subtotals
are their sum, and `python tools/audit_totals_check.py` (battery stage 0)
re-adds them.

| Open item | Weight | What is left |
|---|---|---|
| **A. Engine vs engine** | **0** | |
| B-24r governor tails | 1 | Foreign Investor and Affluence wait on C-38; the Industrialist's reach and the plants' carbon are readings (LAB) |
| B-31r trade-route tails | 1 | the route plunder's base (LAB); the destination's free choice (P8) |
| B-D unsourced data values | 1 | the policy-unlock price's shape, Anarchy's length, the per-city war-weariness split, the durations and "Standard Speed" amounts online (LAB) |
| B-82 the turn-limit score winner | 1 | Empire's era-buildings and Religion's converted terms, wonder districts, the tie direction (LAB) |
| B-86 the emergency combat terms' reach | 1 | where the lost Nuclear emergency's -1 enters, and the anti-air answer to a sortie (LAB, gated on raising an emergency) |
| B-89 fire on a religious unit | 1 | what may target a Missionary / Apostle, and Condemn Heretic's geometry (LAB) |
| B-91 a religion's beliefs | 1 | Religious Colonization's pressure, Holy Waters' reach, "cities following", whose religion a worship building follows (LAB) |
| B-93 Great People as Gathering Storm layers them | 1 | whether Darwin counts the plot underfoot (LAB) |
| B-94 feature plots an improvement refuses | 1 | the improvements with no feature rows that still take a feature, the Volcanic Soil exception (BUILD) |
| **B. Fidelity vs real Civ 6** | **9** | |
| C-1 power | 1 | the accident's building and unit rows (LAB) |
| C-2 diplomatic agreements | 1 | the promise's break and the broken-promise operand (LAB) |
| C-16 the spy's second half | 1 | the counterspy's escape term (LAB) |
| C-20 the route's transportation efficiency | 1 | how the score becomes the multiplier, then the path term (LAB) |
| C-26 civilization abilities, the residue | 1 | Trajan on a capture, the Knarr's Trader path, a damaged city as a wounded foe (LAB); the struck unit's other terms (audit) |
| C-34 air combat's second half | 1 | the interception's strength and damage, whether a shot-down sortie is a battle (LAB) |
| C-38 a city-state's play | 1 | the Foreign Ministry's levy discount, the Logistics project's power, the Training project's Gold, the Food Market / Shopping Mall exclusion (BUILD); quests, builders' improvements, research, centre strength, coastal and naval rates, what its units attack (LAB); later-era starts (no start era) |
| C-41 Volcanic Soil | 1 | the plot condition, bonus-resource loss, Marsh / Oasis, built plots, the yield rows (LAB) |
| C-49 named storms | 1 | the per-step heading law (LAB) |
| C-60 the Free City's own play | 1 | bankruptcy's shape, its defence across eras, its techs, the grant's type rule, a grant with no free tile, the grants' fate on a fall (LAB) |
| C-74 the turn's one random event, the residue | 2 | Kilimanjaro on a Mountain, NoRiver and adjacent terrains for every wonder (BUILD); the empty turns, what a site is, `Spacing`, the first-time boost, the warming form, the drought's start and shield, the fire clock, the meteor's grant, a blizzard on a wonder (LAB) |
| **C. Absent systems** | **12** | |
| **OPEN, TOTAL** | **21** | |

## The question ledger — owner asks

A question the SOURCE under-determines; neither engine ships a branch until
the owner rules or the live game answers. A ruled or measured line is
written into its entry and leaves this list. Numbers are stable (the lab
scenes cite them), so the gaps are closed asks.

| ask | entry | the question | where the answer comes from |
|---|---|---|---|
| 18 | C-2 | DON'T SETTLE NEAR ME's break: how near the promiser must found to break it, if ever. Which seat makes it is read (the founder, promising the seat it settled near); the plain settled-near grievance is 25 within 3 of a rival's border | LAB C-2-S1 (found at border 1 and 2 with the promise standing; read the grievance log in the same turn) |

## A. Engine vs engine

The digest is the only instrument for this class; a round that widens what
the gate reaches is worth more here than one that re-reads the exporter, and
a hunt catch opens an entry here only when it cannot close in the same
commit.

## B. Fidelity vs real Civ 6 — shipped mechanics with open tails

- **B-24r. GOVERNOR TAILS.** Weight 1.
  - LAB (no scene drafted yet): Industrialist and Renewable Subsidizer ship from their rows, with three readings — the plants' +2 Production stays in the governed city (not spread through an Oil or Nuclear plant's regional reach); a city several same-kind plants reach takes the best rate; carbon follows the Power generated (the pedia's "per Power generated"), so a governed plant emits more per resource.
  - BLOCKER C-38: Foreign Investor copies the strategics a minor accumulates and Affluence the luxuries it imports (`*_COPY_*_FOR_IMPORT`); a minor improves nothing here.
- **B-31r. TRADE-ROUTE TAILS.** Weight 1.
  - LAB B-31r-S1: `PLUNDER_ROUTE_GOLD` 50 has no row (no GlobalParameter, no text figure, every pack grepped); the district `PlunderAmount` and the Total War / Letter of Marque percentages ship. Plunders across eras, land and sea, and route yields name the base.
  - P8: the destination is one candidate row plus take/skip; the free-choice head is P8 work.
- **B-D. UNSOURCED DATA VALUES.** Weight 1.
  The online speed ships: `GAME_SPEED` is `GAMESPEED_ONLINE`'s `CostMultiplier` 50, and every cost the install scales (units and their steps, techs, civics, buildings, wonders, projects, districts, the Builder and Settler steps, the worship Faith price) and every `ScaleByGameSpeed` / `Scale` grant (Great People, wonder grants, tribal villages) goes through it, truncated as the lab's purchase record truncates odd costs (`scaleByGameSpeed` / `scale_by_game_speed`). The tile-purchase ring step and the space flight's light years are this engine's readings of the speed.
  A building's purchase price comes off its fractional scaled cost (`buyCost`; all 133 building rows of the lab's purchase record fit it, the units fit the truncated cost). Policy and government changes cost the policy-unlock gold outside the free window, the turn after a civic completes (`policyUnlockCost` / `_policy_unlock_cost`, `GovernmentState.civicTurn` / `civ_civic_turn`), paid by the applier once a turn.
  - LAB: the unlock price's shape — built as 50 on the first paid turn, 5 less a turn after, never under 10 (`CivicUnlockMaxCost`, `PerTurnDrop`, `MinCost`); the other reading is 5 × the turns to the next civic within 10–50. The unlock button read over consecutive turns settles it, and whether the first paid turn costs 50 or 45.
  - LAB: which durations the game scales online. `GameSpeed_Durations` `ONLINE_HALF` maps 30 → 20, 60 → 40, 10 → 8, 15 → 10, but no row a GS game loads names a scaling, so every duration ships at its Standard value (deals, friendships, alliances, denunciations, promises, retribution and competitions 30; emergencies 30 / 60; war and peace minimums 10; the formal war 5; the war buff 10; the research alliance 30; the congress 30 and a special session 15; spy missions; governor establishment; trade routes 20; the levy 30). A friendship's or an emergency's expiry read online settles it.
  - LAB: amounts the text marks "(on Standard Speed)" with no data rule ship unscaled: the strategic stockpile's +10, units' resource costs, kill yields, the raid's +50, Wolin's 25%, Sweden's 50 Favor, conversion science, Great Person point costs, the pantheon's 25 Faith; and whether the game truncates tech, civic and grant amounts as it truncates purchase costs.
  A seat's government is its record's choice (`government`, validated against its civics; `GovernmentState.chosen` / `civ_gov_chosen`), the newest tier the default until it names one; the driver draws a persistent style per seat, so every tier-mate is adopted across games.
  - LAB: Anarchy. `GOVERNMENT_BASE_ANARCHY_TURNS` 2 against the pedia's "a minimum of three turns … longer the more you previously adopted it"; until it is read both engines refuse a return to a government the seat held before, so no seat enters Anarchy.
  - LAB B-D-S0 / S1: the per-city split. `GlobalParameters.xml` carries twelve `WAR_WEARINESS_*` rows and no pack adds one; `LOSS_OVER_REQ_AMENITIES_{AT_WAR_CITY 3, FOUNDED_CITY 0, NONFOUNDED_CITY 1}` may be caps below the city's need rather than addends. The empire-wide rule ships (`warWearinessPenalty` over `wwMax`).
- **B-82. THE TURN-LIMIT SCORE WINNER.** Weight 1.
  Civ 6's Score ships on both engines (`SCORING_LINE_ITEMS`, `scoreLines` / `scoreLeader`, `score_lines` / `leader()`): civics 3, techs 2, wonders 15, Great People 5 per person earned, the game's whole era score 1 per point (`eraScorePast` / `era_score_past` bank each era), Empire 5 per city + 2 per completed district (a wonder's own district included) + 1 per citizen, Religion 5 per belief; past `TURN_LIMIT` the highest total wins, ties broken by the higher `TieBreakerPriority` category, then the lower seat; only majors holding a city score. The digest compares the Score every turn; the `score_victory` lane reaches the winner.
  - LAB B-82-S1: Empire's `LINE_ITEM_ERA_BUILDINGS` residue (10–43), Religion's `Converted` term (the leading reading: other players' cities following it, minors included), whether a wonder's district counts as a district, and `TieBreakerPriority`'s direction (nothing in the install reads the column). A zero-turn re-read of the saved games.
- **B-86. THE EMERGENCY COMBAT TERMS' REACH.** Weight 1.
  - LAB B-86-S0 / S1: the lost Nuclear emergency's `EFFECT_ADJUST_CITY_IDENTITY_PRESSURE` -1 ("Member cities exert one less Loyalty pressure") is read as each member city pressing as one fewer citizen (`citizenPressure` / `_citizen_pressure_from`); the install carries a separate per-citizen effect, which argues for a city-level term. Per-source pressures (`GetCityIdentityPressures`) around a lost emergency decide it; raising an emergency in the lab is itself unproven (S0).
  - LAB B-86-S2: the anti-air answer to a sortie carries no seat-pair term on either engine; the emergency terms require `COMBAT_UNIT_VS_UNIT`, and whether the AA burst is one is read by preview.
- **B-89. FIRE ON A RELIGIOUS UNIT.** Weight 1.
  - LAB B-89-S1: both engines let a melee, ranged, city or barbarian attack take a Missionary or Apostle (`attackTargets` / `hostileRangedStrike`; `_civclass_at` / `_nonbarb_unit_plane`). The pedia: "Religious units are also vulnerable to attack from combat units. Condemn Heretic is an action that combat units may take against them when on the same tile"; both engines condemn from an ADJACENT tile (`A_CONDEMN` + direction). The legality readers answer both.
- **B-91. A RELIGION'S BELIEFS.** Weight 1.
  A religion ends with one belief of each class on both engines (`BELIEF_CLASSES`, `adoptBeliefs` / `_apply_beliefs`): founding takes a Follower and one of Worship, Founder or Enhancer, enhancing takes the classes it lacks; the picks are the record's `beliefs` field and the driver's (`_decide_beliefs`). The nine Worship beliefs and their buildings ship (the Mosque's +1 spread charge, the Dar-e Mehr's pillage immunity, the Gurdwara's Housing, the Pagoda's Favor), built with Production or bought with Faith. REACHED by `tests/gpu/beliefs_test.py` and the `beliefs` lane.
  An Apostle's Evangelize Belief (`EVANGELIZE_BELIEF`) consumes it and earns the next belief (`beliefsEarned`, `RELIGION_INITIAL_BELIEFS` 2 at founding); the nine Founder and nine Enhancer beliefs ship (Lay Ministry, Sacred Places, Missionary Zeal, Monastic Isolation, Holy Waters built; Religious Colonization in the pool, inert); the Dar-e Mehr pays +1 Faith per game era since it was built (`City.buildingEras` / `city_bldg_era`). A seat holds one Great Prophet (`MaxPlayerInstances` 1); the Dar-e Mehr's era Faith counts toward religious healing; the GPU checks a unit's moves for every religious verb as TS does.
  - LAB: Religious Colonization's auto-spread (`EFFECT_ENABLE_RELIGION_AUTO_SPREAD`) names no pressure; Holy Waters reaches any seat's religious units as the install attaches it, against the text's "your"; the Founder beliefs' "cities following this Religion" is counted over the seat's own cities.
  - LAB: whose religion a worship building follows (`EnabledByReligion`): both engines use the owner seat's Worship belief; the city's majority religion is the other reading.
- **B-93. GREAT PEOPLE AS GATHERING STORM LAYERS THEM.** Weight 1.
  The census (`tools/civ6lab/gp_census.py`, every Great Person's layered rows against `GP_ABILITY`) lists what the catalog still misses; the admirals Rajendra Chola, Francis Drake and Ching Shih, Grace Hopper, Einstein's boost and Samori Touré ship as GS layers them, and a Great Person's unit grant takes the civilization's unique replacement (`UniqueOverride`).
  The census's admirals, generals, merchants, engineers and scientists ship as GS layers them (sites incl. City Center, Harbor, Spaceport and a Relic slot; `ActionRequiresNoMilitaryUnit`; unit grants placed map-wide by distance; `GP_BUILDING_YIELDS`; route, loyalty, title, extraction and happiness clauses), with readings: Rockefeller counts distinct improved strategic kinds, Ibn Khaldun's percent compounds on the tier factor, Hanno with nothing unlocked grants nothing.
  Galileo's, Darwin's and Janaki's own sites, the wonder engineers' incomplete-wonder tile, James of St George's missing-Castle gate, James Young's Oil reveal, Mary Leakey's Artifact tourism (her site needing an Artifact), Sun Tzu's Work of Writing and Eisenhower's military-only +5% ship.
  - LAB: whether Darwin's payout counts the natural-wonder plot he stands on (both engines count adjacent wonder plots only).
- **B-94. FEATURE PLOTS AN IMPROVEMENT REFUSES.** Weight 1.
  Solar and Wind Farms, the Polder, the Fishery and the Offshore Wind Farm refuse a feature plot (`noFeature`, from their empty `Improvement_ValidFeatures`); both engines' builder job planes read the owning city's governor promotions.
  - BUILD: the same reading for the rest — City Park, Kurgan, Mission, Stepwell, Mekewap, Chemamull, Golf Course, Ice Hockey Rink, Open-Air Museum, Monastery, Batey and Maori Pa carry no feature row yet take a feature; the Airstrip, Missile Silo, Colossal Heads and Terrace Farm list only Volcanic Soil yet take any; and `noFeature` lets Volcanic Soil through (`bareGround`) where Solar and Wind Farms list no Volcanic Soil row.

## C. Absent systems — the blockers, and the gaps waiting on them

- **C-1. POWER.** Weight 1.
  The NUCLEAR ACCIDENT ships on both engines as measured (`nuclearAccident` / `_nuclear_accident`, `ACCIDENT_*` in `cpu/data/disasters.ts`): three rows of the turn's one event draw (C-74) at `OccurrencesPerGame` 1, one site per major city whose reactor age (`City.reactorAge` / `city_reactor_age`) has reached the row's `MinTurnAtRisk` 10 / 20 / 30; two draws, the Industrial Zone pillaged at DISTRICT_PILLAGED's 0 / 50 / 100, ONE citizen lost at POPULATION_LOSS's 0 / 0 / 80 (never the last), RADIATION_LEAKED's 2 / 10 / 20 turns of fallout on the Industrial Zone's plot alone; no ring improvement pillaged, the plant kept and ageing. REACHED by `tests/cpu/map/disasters.test.ts` and the `power` poke lane; the serve gate reaches it only past Nuclear Fission.
  - MEASURED, lab 4 (`tools/civ6lab/reactor_fleet.py`, `runs/reactor_20260923T192129Z.jsonl`): 75 forced accidents, 25 per severity: the Industrial Zone pillaged 0/25, 13/25, 25/25; one citizen lost in 22/25 cities at CATASTROPHIC, none at MINOR (the record's seven MINOR losses came after a turn: a socket-set population snapping back, and two turns passing); the fallout on the reactor's own plot, 2 / 10 / 20 turns; no building removed (0/80) and no farm within 3 pillaged (0/40).
  A Free City's reactor keeps ageing and is a site (`ageReactors` / `_age_reactors` from `freeCitiesPhase` / `_free_cities_phase`); a flip carries the reactor's age.
  - LAB C-1-S1: BUILDING_PILLAGED (MINOR 20, MAJOR 100) — lab 3's InGame read, taken correctly, pillaged the Factory and Power Plant and spared the Workshop at MAJOR, so per building or the plant alone is open (the "`IsPillaged` throws" reading was the `ok and v or "err"` idiom printing a false); and the UNIT_DAMAGE / UNIT_KILLED_CIVILIAN / CITY_GARRISON rows (MAJOR 50, CATASTROPHIC 100, damage 20–50), which neither engine applies.
  A city-state's grid is powered by renewables alone (`minorPower`, `_minor_power`): it holds no stockpile and builds no plant, so no reactor ages there.
- **C-2. DIPLOMATIC AGREEMENTS.** Weight 1.
  The promises ship on both engines: the four `DiplomaticActions_XP2` rows (`cpu/data/promises.ts`, `eras.promises`), a ledger per ordered pair (`promiseWith` / `seat_promise`, `promiseBrokenWith` / `seat_promise_broken`), the ask and the answer settled in one turn (`settlePromises` / `_settle_promises`), the incursion (`promiseIncursion` / `_promise_incursion`) called from the offensive spy mission, the city that comes to follow the promiser's religion and the dig worked on the asker's ground, and the War of Retribution's `brokenPromise` reading the window. The plain "settled too near" grievance ships (`grievanceSettledNear` / `_grievance_settled_near`, from `foundCityAt` / `_found_city_at`): every other major owning a plot within 3 of the new centre gains it against the founder. REACHED by `tests/cpu/seats/promises.test.ts`, `grievances.test.ts` and the `promises` poke lane; the serve gate through the driver's `_promise_turn`.
  `GRIEVANCE_SETTLED_NEAR` is 25: the lab read 19 and 18 one turn after the act, after the Industrial (6) and Renaissance (7) `GrievanceDecayRate`.
  - LAB C-2-S1 (ask 18): the break's reach. The watches never saw a break — every promise lapsed after about 30 turns — and the founding scene tried border distances 3–6 only. Found at border 1 and 2 with the promise standing.
  - LAB C-2-S1: `GRIEVANCE_MULTIPLIER_FOR_BROKEN_PROMISE` 200 names no operand; both engines charge the notification's literal 100. The grievance log (`GetGrievanceLogEntries`) read in the turn of a break gives the amount.
  - Model lines, identical on both engines and kept as notes: a luxury has no lump to trade (the install trades ACCESS, never an amount); the intel bonus is unit-against-unit only; one running deal per ordered pair, `DEAL_ITEMS` a side, an offer standing two turns; a war does not end a standing deal or promise; the Third Party War's "another player" is read as an ALLY; a refused ask stands the pedia's 30 turns; the refused asker's favor comes back at the refusal, where the pedia says "refunded for the next session"; an offensive mission is an incursion whatever its outcome; an ask is answered in the turn it is made; an ask the asker cannot make is dropped, neither paid nor refused.
- **C-16. THE SPY'S SECOND HALF.** Weight 1.
  - The escape ships on both engines as measured (`spyEscape` / `_spy_escape`; lab 4, `spy_loop.py`, 129 escapes): the police guess one offered route uniformly, and the spy gets away when the 3d6 lands at or under 10 + its level (-4 on a right guess). One-route cities let 10 of 54 away (14.4 predicted); two-route cities 41 of 63 (32.9 predicted, about +2σ); a lost escape ended in the cell 34 times in 117.
  A counterspy guards its district and every adjacent one (`LOC_ESPIONAGECHOOSER_COUNTERSPY`; read across cities), and the highest-level post pursues (`counterspiesGuarding` / `_counterspies_guarding`).
  - LAB C-16-S1: `ESPIONAGE_ESCAPE_COUNTERSPY_LEVEL_MODIFIER` -1 per level is applied as written, unmeasured: all 234 prompts named the police. A counterspy posted by a second seat, read at the prompt.
- **C-20. THE ROUTE'S TRANSPORTATION EFFICIENCY.** Weight 1.
  Routes walk through Mountain Tunnels and Qhapaq Ñan, one portal network per range (`portalExit`, `tradeWalkable` / `tradeWalkStep`, `_trade_walk_step`).
  - LAB C-20-S1: the pedia multiplies a route's yields by its path "over water or Railroads, or through Canals or Mountain Tunnels", from `TRADE_ROUTE_TRANSPORTATION_EFFICIENCY_MAX_RATIO` 1.0, `_SCORE_BEST_ROUTE_TILE` 2, `_SCORE_MULTIPLE_DOMAINS` 15, `_SCORE_PORTAL_USE` 15, `_SCORE_WATER_TILE` 2 (`Expansion2_GlobalParameters.xml`). How the score becomes the multiplier (Σ/100 capped at `MAX_RATIO`, or normalised by length; floor or round) is read from the route-chooser previews; then the path term is built on both engines.
- **C-26. CIVILIZATION ABILITIES — THE RESIDUE.** Weight 1.
  The census is `docs/roster_ledger.json`, read as `docs/ROSTER.md` says (354 of 356 rows `shipped`, two `AI:`). A city's strike composes the struck unit with the roster's rows (`cityStrikeDefenderCS`, `_seat_city_strike`); no roster row carries an attacking or defending requirement, so one composer serves both sides. Qhapaq Ñan ships (`MOUNTAIN_ROAD`), and a wonder takes floodplains as GS allows (`Features_XP2.ValidWonderPlacement`).
  - LAB: whether a damaged city is a "wounded" opponent (`REQUIREMENT_OPPONENT_IS_WOUNDED`, Tomyris); both engines read a city as never wounded.
  - The city strike's defender still omits `defenderCS`'s other unit-vs-unit terms (`chassisAbilityCS`, `barbarianCombatCS`, `visibilityCS`, `allianceWarCS`, `eraMatchupCS`); audit which apply to a struck unit before building.
  - LAB C-26-S1: whether Trajan's grant (`TRAIT_ADJUST_NON_CAPITAL_FREE_CHEAPEST_BUILDING`, no requirement set, no RunOnce) fires on a CAPTURED city; founding ships (`trajansColumn` / `_trajans_column`).
  - LAB C-26-S3: whether Norway's Ocean clause (`TRAIT_EARLY_OCEAN_NAVIGATION`, Ocean valid after Shipbuilding) reaches a Trader's water path before Cartography (`tradeWaterLevel`).
- **C-34. AIR COMBAT'S SECOND HALF.** Weight 1.
  Patrol, Priority Target and the sortie's order ship on both engines. A fighter deploys over a hex within its Moves of its base (`deployAir` / the `DEPLOY_k` columns, which offer the seat's own district tiles; Return to Base costs no Moves) and holds it until another air order; it intercepts a sortie aimed within 1 of that hex, the strongest patrol fighting with +5 per other (`INTERCEPT_RANGE`, `INTERCEPT_SUPPORT_CS`, pedia); an intercepted fighter aborts, a bomber flies on; stationed aircraft never intercept, and a patrol heals only with Ground Crews. Priority Target strikes the tile's Support-class unit past its combat unit (`priorityTargets` / `_priority_targets`). A sortie meets interception, then the anti-air cover, then strikes only if it survives, the bomb's 50% check asked again after the answers; a bomber striking a city meets both (`airAnswers` / `_air_answers`). REACHED by `tests/cpu/units/air-patrol.test.ts` and the `air_patrol` lane (no seed trains aircraft).
  - LAB C-34-S1: the interception's strength and damage by preview — both engines reuse the anti-air answer's body (one roll, the attacker alone damaged, no XP to the interceptor, the P-51's +5 vs fighters not applied), and pick the interceptor by base strength against aircraft. Also: whether a sortie ended by its answers counts as a battle for war weariness and XP (neither engine counts it), and the bomb's boundary (the Bomber page's "more than 50%", shipped, against the chapter's "50% health or higher").
- **C-38. A CITY-STATE'S PLAY.** Weight 1.
  OWNER 2026-09-23: a city-state is no agent, so its behaviour IS the environment an agent trains against, and a gap an agent can exploit there is a sim-to-real gap. Every observable city-state behaviour is therefore an engine rule: built from the install where it publishes rows, measured in the lab where it does not, and randomised per episode where neither settles it, so no policy leans on one guessed value. Its food and culture already ride the majors' composers.
  - MEASURED, 262 watched games (`tools/civ6lab/runs/cs_watch_*.jsonl`: lab4, obs1–6, cfgA–E, s2n1–5; 35,081 city-state turns, every one an Ancient start; re-matched with the install's `UnitUpgrades` chains): every minor starts with a Settler and two Warriors. FIRST BUILD a Monument (213 of 250) or a Slinger (37); then a Builder (median turn 4), Warrior (16), Granary (20), Archer (29), Trader (35); walls in 230 of 250 at median turn 63; the type's district in 157 of 250 (Scientific 36/40 at 27.5, Religious 32/39 at 26, Cultural 29/42 at 58, Trade 26/40 at 57.5, Industrial 20/45 at 87.5, Militaristic 14/44 at 98.5), never another type's; Harbor, Water Mill, Castle, Star Fort, Neighborhood, Sewer and the district projects later; 21.7% of turns to t101 build nothing. TRAINED 2,321 units (Builder 636, Warrior 502, Archer 333, Trader 307 …). BUYS: 453 Builders (452 with no Builder standing, bank ≥ 93, median 115 spent; 3–12% a turn with a bank of 100–300), 203 military units (bank always ≥ 95, median share left 0.24; 1.7% a rich turn at 0–1 units, 0.4% at 6–7, 0 of 348 at 8+; tripled within 3 turns of a loss), 2,083 upgrades (about 4.3 gold each — `UPGRADE_MINIMUM_COST` 15 is the floor candidate — despite the 100% discount), faith 7 times in 262 games (Warrior Monks). Builders stay within 2 of the centre 94% of turns. Land military at peace: distance from the centre d0 8%, d1 21%, d2 22%, d3 18%, d4 15%, d5 11%, 6+ 5%; at war d0 17%, d1 25%, d2 24%, d3 13%; a turn's step 0: 19.5%, 1: 56.7%, 2: 20.6%, 3+: 3.2%.
  - SHIPPED, both engines: the Ancient starting army (two Warriors from `loadWorld`, riding the GPU's majors' pool under 100 + id), which holds, digs in, heals, defends and is removed with the minor's conquest; the walled minor's city strike (`cityStrikes` from `minorPhase`, `_city_strikes` from `_city_state_phase`); the production rows (`MINOR_CIV_PRODUCTION_PENALTY` -50% and the toward-rows, measured to MULTIPLY: x1 plain, x3 walls / Builder / military, x6 the type district; `minorProduction`, `toward`). REACHED by `minor-army.test.ts`, `minor-strike.test.ts`, `minor-record.test.ts`, the `minor_record` and `minor_builds` lanes and the serve gate.
  - SHIPPED, both engines: ITS BUILD ORDER AND TRAINING, fitted to the census (`tools/civ6lab/minor_build_fit.py`): `MINOR_BUILD_ROWS` (Builder while none stands, Monument, melee while the army is under 3, Granary, a ranged unit while none stands, the type's district, tier-1 building, Water Mill, Harbor, walls, Lighthouse, tier-2 building, the later walls, Military Academy, Shipyard, Neighborhood, Sewer, Seaport, and an army row under a per-episode cap), each drawn row's timing drawn once per episode (`minorPlan` / `_minor_plan`); the army row trains the class the army holds fewest of against the census weights; the Builder and military production rows now reach their units. A minor's Builder improves a random valid (plot, improvement) pair on its land within 2 of the centre at the measured 236‰ a turn (`minorBuilders` / `_minor_builders`). The quest's camp radius is 5; the GPU's walls tier reads the minor's Urban Defenses as TS does.
  It runs TRADE ROUTES from its city (the Trader row at the census's timing; the destination the engine's own scorer, minors first, then majors; `minorTrade` / the GPU's minor trade), builds the district projects, the wall repair, worship buildings, the Flood Barrier and a Food Market, and buys naval units at the census's coastal rate (`MINOR_NAVAL_BUY_BP`, a lab estimate). A district on an improved plot removes the improvement, feature and bonus resource for every seat (`paveGround` / `_pave_plot`). The levy hands the minor's own military to the suzerain for `LEVY_TURNS` 30 at 25% of their purchase cost (`LEVY_MILITARY_*`), and returns them.
  - BUILD: the Foreign Ministry's 50% levy discount; the Logistics project's `FullyPoweredWhileActive`; the Encampment Training project's 15% Gold; the Food Market's exclusion with the Shopping Mall (no Shopping Mall in the catalog).
  - SHIPPED, both engines: ITS PURSE AND ITS WALK (`tools/civ6lab/minor_play_census.py`): a Builder bought when none stands at a per-episode rate (about 8% a turn), a military unit bought above a 95-gold floor at a rate falling with army size (0 at 8+, ×3 within 3 turns of a loss), one upgrade per completed tech or civic at 5 gold (`UPGRADE_BASE_COST` × speed), a Warrior Monk for faith where the belief allows; the minor pays unit upkeep and its treasury floors at 0; it ignores strategic requirements (`CivilizationLevels`); its land units walk the measured step and distance tables for peace, war and damage (`cpu/core/walker.ts`, `_walk_units`).
  A city-state's city holds the Palace (`BUILDING_PALACE` `Capital`, each minor a `CapitalName`): the census's +5 Gold a turn from founding, with the Palace's Production, Science, Culture, Housing and Amenities.
  - LAB C-38-S1: the quest pool (eight kinds in `Quests.xml`, no weights; `QUEST_COOLDOWN` 12 and `QUEST_ENVOYS` 1 unsourced), what builders improve, the research order, and unit tracks by id — an extended observer watch. The same watch reads the Harbor, Water Mill and Lighthouse rates over coastal and river minors (the census has no such denominator), and why the engines' minors lag the census (a Monument at turn 25–42 against 15; about 9 techs by turn 150). What its units attack at war (the census records no combat).
  - LAB C-38-S2: the centre strength. `minorCityCS` / `_minor_centre_cs` read 15 + population + 6 (militaristic) with no row; the install's only minor strength is `StartEras.StartingMeleeStrengthMinor` (25 at Ancient), and a garrison adds nothing where a major's centre takes +5.
  - BLOCKER, no start era: `BonusMinorStartingUnits`' later-era rows (Classical to Information, GS swapping Pikeman for Pike and Shot) wait on a later-era start existing on either engine.
  - LAB: the naval-buy rate (31 buys over 4,979 coastal-evidence minor turns) and the Research Lab, Stock Exchange and Broadcast Center rows the census never saw built.
- **C-41. VOLCANIC SOIL.** Weight 1.
  Both engines paint the RADIUS-1 RING as measured (`erupt` / `_erupt`): one draw per eligible plot in ring order (`soilPaintable` / `_soil_paintable` — land, no Mountain, no district, city centre or wonder, bare or Woods / Rainforest), Volcanic Soil at the severity's YIELD_FOOD row (35 / 50 / 75), replacing the Woods or Rainforest and a Lumber Mill; then they scorch (pillage) and fertilize the ring. The radius is sourced: `RealismSettings.ExtraRange` is false at MODERATE. REACHED by `tests/cpu/map/feature-add.test.ts` and the `feature_add` lane.
  The eruption's `RandomEvent_Damages` rows ship (`ERUPTION_*` over the five eruption rows, `eruptTile` / `_erupt_tile`): pillage, `IMPROVEMENT_DESTROYED`, `DISTRICT_PILLAGED`, `BUILDING_PILLAGED`, the city centre, `UNIT_DAMAGE_LAND` with its band, `UNIT_KILLED_CIVILIAN` and `POPULATION_LOSS` (only where the plot has an owning city), on every ring plot whoever owns it, with six draws per plot.
  - MEASURED, lab 4 (`volcano_scene.py`, `runs/volcano_20260923T191337Z.jsonl`, 78 eruptions): the paint is a per-plot chance applied at once — bare land 30 / 52 / 74%, Forest and Jungle 6/28, 11/28, 18/28, Floodplains and Geothermal Fissure never. The damage switched ON per plot as the run went on and then followed the rows (GENTLE pillage 3/3; CATASTROPHIC 8 destroyed + 1 pillaged; MEGACOLOSSAL 12 + 2); strategic and luxury resources were never lost (0/42), bonus resources on 21/21 painted and 9/21 unpainted plots.
  - LAB C-41-S1: what switches the damage on (plot ownership is the leading candidate: the scene laid improvements with owner -1); whether only bonus resources are lost and on which plots; whether Marsh and Oasis (`ValidForReplacement`) are painted; a district, centre or wonder on the ring; and the `YIELD_PRODUCTION` (15/25/35) and `YIELD_SCIENCE` (10/15) rows under Volcanic Soil, read by neither engine.
- **C-49. NAMED STORMS.** Weight 1.
  `stormWalk` / `_storm_walk` ship the measured model: eight unit steps on the movement turn from the `PrevailingWinds` band at the centre's current latitude, eight more with no footprint on dissipation.
  - LAB: the per-step law. The 31 resultants measured fit no single-heading walk, and the records carry a per-storm `CurrentDirection` set at spawn on the band's side and changing each turn — a persistent-heading walk is the alternative. No mid-turn read exists (the tuner runs between turn processing). First the natural storms' 16-step resultants from the save sweep (C-74-S1), then C-49-S1's batch hurricanes if they do not separate the laws.
- **C-60. THE FREE CITY'S OWN PLAY.** Weight 1.
  The seat is in on both engines (revolt, race, join, Eleanor's skip, open to attack, the religion walk). Its city stands on a flat 72 (`FREE_CITY_DEFENSE`; `holderStrength` / `_holder_base`, measured once, Medieval, no walls, no garrison); a walled Free City fires the ordinary strikes; the centre heals at the ordinary rate and its loyalty restarts at 100. REACHED by `tests/cpu/city/free-city.test.ts` and the `free_city` lane.
  - MEASURED, 28 flips over the watched games (`cs_watch_*`, `rebel_watch_rebel3b/3c`): the revolt's pair is the era's melee (Swordsman, Man-at-Arms, Musketman, Line Infantry, Infantry, Mechanized Infantry — within a step of the minors' best melee at that turn); then a unit EVERY 5 turns while the city stays Free, of any class and the era's type (Horseman, Archer, Courser, Crossbowman, Cavalry, Helicopter, Field Cannon, Machine Gun, Musketman, Line Infantry, Infantry, Ranger, Mechanized Infantry, and Builders), on the same tile; it TRAINS (Slingers, Warriors, a Trebuchet; walls, a Castle, districts and repair projects in its queue); its player holds gold (0 on 223 of 296 city-turns, up to 120, falling 4–5 a turn with upkeep); its units move (d0 5%, d1 60%, d2 28%, d3+ 7%; a turn's step 0: 48%, 1: 17%, 2: 17%, 3: 14%); on a join the granted units near the city vanish the same turn, the trained ones stay Free.
  BANKRUPTCY ships for every seat, the Free Cities and city-states included (`GOLD_NEGATIVE_BALANCE_*`, `bankruptAmenities` / `bankruptDisbands`, `_bankruptcy_count`): each city loses 1 amenity once the treasury drops below 0 and one more per further 10; at -10 one unit disbands and one more per further 10, the priciest first, never a free unit; the treasury may go negative. The Free Cities play ships too: the revolt grants two of the world era's melee unit (`eraUnitOfClass` walks the class's upgrade chain by unlock era), a unit every 5 turns while the city stays Free drawn over the census's class mix (`FREE_CITY_GRANT_WEIGHTS`, `tools/civ6lab/free_city_census.py`), a treasury (`freeSeat.treasury` / `free_treasury`) banking its cities' gold and paying its units' upkeep, and on a join the city's own grants (`Unit.freeCity` / `unit_free_city`) are removed.
  It trains and builds from `FREE_CITY_BUILD_ROWS` (a ranged unit, Monument, Granary, walls, the repair project, a siege unit, district buildings), its research read as every tech and civic of the world era, and its units walk the minors' body with its own tables. Its treasury floors at 0 as the minors' does, and bankruptcy reaches the majors alone.
  - LAB C-60-S2 also: its own techs (Slingers at turns 90–114 where the world era's research gives Archers); its districts (4 of 28 records), the Flood Barrier and district projects are not hosted.
  - LAB C-60-S1: whether the 72 follows the era, and whether walls and a garrison add to it (zero-turn reads of the Free Cities standing in the saved games).
  - LAB C-60-S2: the flip pair's rule (world era, the former owner's best melee, or its own techs), the grant's type draw, the grants' fate on a join or fall (removed or transferred), its income, its walker's targets.
  - LAB C-60-S3: a grant with every ring tile occupied.
  - LAB C-60-S4: bankruptcy's shape over the turns of insolvency (per city or empire, whether the balance goes negative — no read below 0 in 35,000 rows — and which unit goes). Both engines read the pedia's "below 0" strictly (a seat at 0 loses nothing) and its "at -10" inclusively; open: whether the game clamps the balance at 0 and counts the shortfall instead (the lab read a loss of 1 at a Free City holding 0).
- **C-74. THE TURN'S ONE RANDOM EVENT, THE RESIDUE.** Weight 2.
  Both engines fire at most ONE random event a turn (`randomEvent` / `_random_event`, from `disasterPhase` / `_disaster_phase`): one draw over the eligible (row, site) pairs in the install's `RandomEvents` order, each pair weighted by its row's `OccurrencesPerGame` at MODERATE (`eventRows` / `_event_rows`) — the three floods once per river carrying Floodplains (`floodSites` / `_flood_sites`), Kilimanjaro's two eruptions once per Mount Kilimanjaro plot, the three eruptions once per volcano, the eight storms once each while their start terrain exists, the three nuclear accidents once per reactor past `MinTurnAtRisk` (C-1), and the two droughts once while Plains or Grassland exists. The drawn row IS the severity; a storm's or a drought's centre is a second draw. The spy's breached Dam draws its flood's severity by the same weights (`floodSeverity` / `_flood_severity_draw`). REACHED by `tests/cpu/map/disasters.test.ts` and the `storms`, `feature_add`, `flood_severity` and `power` lanes.
  - MEASURED, the event histories (19 games, `runs/event_history_*`): one slot a turn; the empty share falls with map size (Small 25%, Standard 8%, Large 1%, Huge 0%) and one-site rows run at about `OccurrencesPerGame` × turns / 250 × 0.75 / 0.93 / ~1.2 by size — the column is an absolute per-site rate, not only a weight; lab 4's 74 eruptions fit 70% of its 13 volcanoes; the flood mix held at base through seven sea-level rises while the severity-2 storm share and drought EXTREME rose, as the `ChanceIncreasePerDegree` columns say; no strict 15-turn cooldown per row (19 CAT_4 in 249 turns).
  The drought, the start turn and the warmed weights ship: a drought starts on featureless Plains / Grassland, pillages the listed improvements (`DROUGHT_IMPROVEMENTS`, destroyed at EXTREME's 30%), bans rebuilding and repairing them while it lasts, and spares the food of a city holding an unpillaged Aqueduct (or Bath), Dam or Stepwell (`droughtShielded` / `_drought_shield`); no event fires before `RANDOM_EVENT_START_TURN` 2; each row weighs `OccurrencesPerGame` × (1 + `ChanceIncreasePerDegree`/100 × ΔT), ΔT the world's carbon over `CO2For1DegreeTempRise`, continuous (`warmedWeight` / `_warmed`).
  The Gathering Storm pack rows ship (`GranColombia_Maya_Expansion2.xml`, `VikingsLandmarks_Expansion2.xml`, criteria `RULESET_EXPANSION_2`), in the live game's `RandomEvents` order: METEOR_SHOWER (one site on unowned, unimproved Plains / Grassland / Snow / Desert; its Meteor Site is claimed like a village for the next Heavy Cavalry line unit, which pays no resource upkeep), FOREST_FIRE and JUNGLE_FIRE (burning, burnt and regrowing Woods and Rainforest, 50% spread, the damage band, fertility), Eyjafjallajokull's two eruptions and VESUVIUS_MEGACOLOSSAL (their ring every plot touching the wonder). A decision-free TS / GPU run of the disaster phase matched turn for turn on seven seeds.
  Eyjafjallajokull and Vesuvius stand in the natural-wonder roster (`world/features.ts`, `world/wonders.ts`); a wonder's terrain list and inland rule hold for every plot it covers. In the locked worlds Vesuvius stands on five seeds (9027, 9209, 9235, 9287, 9300) and Eyjafjallajokull on none — the placement rules put it on about 5% of maps.
  - BUILD: Kilimanjaro stands on flat Grassland / Plains where its `Feature_ValidTerrains` names a Mountain; `NoRiver` is read for the two new wonders only; no wonder reads `Feature_AdjacentTerrains`.
  - LAB: whether a Blizzard starts on a Tundra wonder plot (`stormFamilyAt` reads terrain alone).
  - LAB: the fire clock (regrowth at turn 6 by the Yields row, 8 by the notification; whether spread plots share the event's clock; whether a Forest Fire spreads into Rainforest); the meteor's grant ("a more powerful unit" of the class) and who may claim a site; the fire features are `ValidForReplacement` yet an eruption does not paint them (with Marsh and Oasis, C-41).
  - LAB C-74-S1 / S2: the empty turns — the normaliser and the map factor, measured at Duel, the engine's own size; what a site is (active volcanoes under `PercentVolcanoesActive` 70; a river or a floodplain plot); `Spacing` 15 (a distance or a gap); `RANDOM_EVENT_FIRST_TIME_OCCURRENCE_BOOST` 30. Also: whether warming steps by half degrees or can fall (both engines read ΔT continuous); the drought's start (the tooltip's "devoid of all Features", shipped, against the pedia's "four featureless Grassland/Plains tiles adjacent"); whether a pillaged shield protects and which plots count as the city's.

## Harness — not weighted

- **A-52. SWITCHES THAT ARE ALWAYS ON, AND RULE READS WITH DEFAULTS.**
  - BUILD: `GOVERNMENTS_ADOPTION_LIVE`, `CITY_RELIGION_ADDER_LIVE`, `BARB_SCOUT_OPENER_LIVE`, `ADMIRAL_MARCH_LIVE`, `DEDICATION_PAYOUTS_LIVE`, `ENGINEER_LIVE`, `SCRIPTED_CAMPUS`, `B18_FOLLOWER_COUPLING_LIVE` and `embarkState.live` are all `true` in play (`setEmbarkLive` and two GPU tests' `_gov_has_effects = False` switch arms off for tests only). Delete each flag, its arms, its key and its switch-off test.
  - BUILD: keys exported and applied by neither engine: `admiralMarchLive`, the campus scaffold's `active` and `campusUnlockTech`, and `formalWarMinTurns` (the GPU reads it into `_formal_war_min` and never uses it; both engines take the formal war's 5 turns from `WAR_KINDS`). Stop exporting them and delete the reads; the other war kinds' literal 5s read `FORMAL_WAR_MIN_TURNS`.
  - BUILD: the GPU's `improvements_on` / `districts_on` gate sections the exporter always ships, and about 590 `.get("key", <number>)` rules reads in `gpu/core` (about 167 with a real magnitude) carry a second copy of each value. Index the rules directly so a missing key fails loudly.
- **A-55. WHAT THE HARNESS STILL CARRIES FROM OLD SHAPES.**
  - BUILD: `deserialize` (`cpu/core/game.ts`) keeps a long block of old-save `??=` back-fills; the untracked preset worlds (`seeder/worlds/presets/*`) carry `aggression` and a stale genStamp (regenerate or delete them).
  - BUILD: the GPU cannot order a ship's move onto water (the residual `naval_test` documents), and TS has no route planner since `findPath` went.
