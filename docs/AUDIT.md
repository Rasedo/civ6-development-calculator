# Engine audit — open work

THIS FILE IS A LIST OF OPEN WORK. A resolved entry is DELETED, not
annotated; what was fixed, when and why is the git log's job (`git log -S
<id>` finds an entry's history). One weight and one OPEN list per entry;
every bullet is one of four things and says which:

- **BUILD** — buildable now from a sourced rule; names the symbols.
- **BLOCKER** — waits on another entry; names it.
- **ASK** — the source under-determines it; the owner rules (the ledger below).
- **LAB** — the live game answers it (`tools/civ6lab/SESSION5.md`, scene named).
  A rule the install carries no number for is inferred from what the live game
  does: a scene that varies its inputs and fits the observed outcome.

**RULES (owner):**
- Anchor code BY SYMBOL, never by line number.
- VERIFY-BEFORE-IMPLEMENT against a real Civ 6 source: the owner's install
  (XML over civilopedia; Base <- Exp1 <- Exp2, take the last) first, the
  live game second, forums last. An unsourced magnitude is an ASK, never an
  invention.
- A gap deferred on an unbuilt mechanic is TWO open items: the mechanic and
  the gap naming it. "Recorded" and "descoped" are deferrals, never closures.
- Every new mechanic records which lane REACHES it; a green gate over an
  unreached mechanic proves nothing.
- When an entry closes, delete its row and its entry in the SAME commit.

**State:** P8 training PARKED until this file is empty.

## Open weight

Hand-weighted 1–8 by the size of what is LEFT to build. One row per entry,
no row without an entry. The rows are the source of truth; the subtotals
are their sum, and `python tools/audit_totals_check.py` (battery stage 0)
re-adds them.


| Open item | Weight | What is left |
|---|---|---|
| **A. Engine vs engine** | **0** | |
| B-24r governor tails | 1 | Industrialist and Renewable Subsidizer (BUILD, rows named); Foreign Investor and Affluence wait on C-38 |
| B-31r trade-route tails | 1 | the escort's radius 4 and the admirals' naval-only bonus (BUILD); the route plunder's base (LAB); the destination's free choice (P8) |
| B-D unsourced data values | 2 | the online speed's cost multiplier 50 and its scaling tables (BUILD); a seat's government choice (BUILD); the per-city war-weariness split (LAB) |
| B-82 the turn-limit score winner | 2 | the Score and its winner on both engines (BUILD); Empire's era-buildings and Religion's converted terms (LAB) |
| B-85 Exodus of the Evangelists' golden movement | 1 | the Inquisitor takes it (BUILD) |
| B-86 the emergency combat terms' reach | 1 | where the lost Nuclear emergency's -1 enters, and the anti-air answer to a sortie (LAB, gated on raising an emergency) |
| B-87 the Marae's dedication event | 1 | only a building with a Great Work slot fires Pen, Brush and Voice (BUILD) |
| B-89 fire on a religious unit | 1 | what may target a Missionary / Apostle, and Condemn Heretic's geometry (LAB) |
| B-90 citizen loyalty pressure | 1 | the capital's +1 and the ages' ±0.5 per citizen (BUILD) |
| **B. Fidelity vs real Civ 6** | **11** | |
| C-1 power | 1 | a Free City's reactor ages and is a site (BUILD); the accident's building and unit rows (LAB); a minor's grid (C-38) |
| C-2 diplomatic agreements | 1 | the settled-near grievance is 25 (BUILD); the promise's break and the broken-promise operand (LAB) |
| C-16 the spy's second half | 1 | a counterspy guards adjacent districts, the highest post pursues (BUILD); the counterspy's escape term (LAB) |
| C-20 the route's transportation efficiency | 2 | trade routes walk through Mountain Tunnels, the five efficiency rows sourced (BUILD); how the score becomes the multiplier (LAB) |
| C-26 civilization abilities, the residue | 2 | the city strike takes the roster's rows, Qhapaq Ñan, the census's missing rows (BUILD); Trajan on a capture and the Knarr's Trader path (LAB) |
| C-34 air combat's second half | 3 | Patrol (deployed interception) and Priority Target from the pedia (BUILD); their strengths (LAB) |
| C-38 a city-state's play | 6 | training, build order, purchases, upgrades and a first walker from the watched games; the quest radius; the walls-tier fork (BUILD); quests, builders' improvements, research, centre strength (LAB); later-era starts (no start era); its grid (C-1) |
| C-41 Volcanic Soil | 2 | the eruption's unapplied damage rows (BUILD); the plot condition, bonus-resource loss, Marsh / Oasis, built plots, the yield rows (LAB) |
| C-49 named storms | 1 | the per-step heading law (LAB) |
| C-60 the Free City's own play | 3 | bankruptcy for every seat, the recurring grants, its training and treasury (BUILD); its defence across eras, a grant with no free tile, the grants' fate and its walker (LAB) |
| C-74 the turn's one random event, the residue | 4 | the drought's rules, `RANDOM_EVENT_START_TURN`, the per-degree columns, the Gathering Storm pack rows (BUILD); the empty turns, what a site is, `Spacing`, the first-time boost (LAB) |
| **C. Absent systems** | **26** | |
| **OPEN, TOTAL** | **37** | |

## The question ledger — owner asks

A question the SOURCE under-determines; neither engine ships a branch until
the owner rules or the live game answers. A ruled or measured line is
written into its entry and leaves this list. Numbers are stable (the lab
scenes cite them), so the gaps are closed asks.

| ask | entry | the question | where the answer comes from |
|---|---|---|---|
| 18 | C-2 | DON'T SETTLE NEAR ME's break: how near the promiser must found to break it, if ever. Which seat makes it is read (the founder, promising the seat it settled near); the plain settled-near grievance is 25 within 3 of a rival's border | LAB C-2-S1 (found at border 1 and 2 with the promise standing; read the grievance log in the same turn) |

## A. Engine vs engine

The digest is the only instrument for this class; a round that widens what
the gate reaches is worth more here than one that re-reads the exporter, and
a hunt catch opens an entry here only when it cannot close in the same
commit.

## B. Fidelity vs real Civ 6 — shipped mechanics with open tails

- **B-24r. GOVERNOR TAILS.** Weight 1.
  - BUILD: Industrialist and Renewable Subsidizer ship `{}` (`cpu/data/governors.ts`) while the plants they act on ship. Rows (`DLC/Expansion2/Data/Expansion1_Governors.xml`): `INDUSTRIALIST_{COAL,OIL,NUCLEAR}_POWER_PLANT_PRODUCTION` (+2 Production on each plant), `INDUSTRIALIST_RESOURCE_POWER_PROVIDED` (Amount 1); `RENEWABLE_ENERGY_IMPROVEMENT_PLOTS_GOLD` (+2 Gold on Solar / Wind / Offshore Wind / Geothermal plots), `RENEWABLE_ENERGY_IMPROVEMENT_BUILDING_GOLD` (+2 Gold on the Hydroelectric Dam); the +2 Power rows `MERCHANT_RENEWABLE_ENERGY_*` (`Expansion2_Buildings.xml`, `Expansion2_Improvements.xml`), gated on the promotion. Into the two `G(...)` rows and `cityPower` / its GPU twin.
  - BLOCKER C-38: Foreign Investor copies the strategics a minor accumulates and Affluence the luxuries it imports (`*_COPY_*_FOR_IMPORT`); a minor improves nothing here.
- **B-31r. TRADE-ROUTE TAILS.** Weight 1.
  - BUILD: the Mandekalu / Bireme escort guards a Trader within 4 (`AOE_TRADER_REQUIREMENTS` → `TRADER_IS_WITHIN_FOUR_REQUIREMENT`, `Expansion2_UnitAbilities.xml`; the text "within 4 tiles"); both engines guard only the Trader's own tile and its ring (`routePlunderer`, `pair_dist <= 1` in `sim_seats.py`).
  - BUILD: the admirals' plunder bonus is a unit ability of the naval classes only (`UnitAbilities.xml`, "+X% rewards for plundering sea Trade Routes"); both engines multiply every raider's payout by `routePlunderPct` (`phase.ts`, `_gp_perm_at`).
  - LAB B-31r-S1: `PLUNDER_ROUTE_GOLD` 50 has no row (no GlobalParameter, no text figure, every pack grepped); the district `PlunderAmount` and the Total War / Letter of Marque percentages ship. Plunders across eras, land and sea, and route yields name the base.
  - P8: the destination is one candidate row plus take/skip; the free-choice head is P8 work.
- **B-D. UNSOURCED DATA VALUES.** Weight 2.
  - BUILD: the online speed. `GameSpeeds.xml` `GAMESPEED_ONLINE` `CostMultiplier` 50 (the lab's purchase record matched ×0.5 on 302 of 302 rows); both engines cost at `GAME_SPEED` 0.6. Set it from the row, then check every other use against `GameSpeed_Scalings` (`ONLINE_HALF` 66, `ONLINE_SLIGHT` 80) and `GameSpeed_Durations` (`ONLINE_HALF`: 5→5, 10→8, 15→10, 29→19, 30→20, 60→40): `builderBase`, `settlerBase`, `worshipFaithCost` in `rules.ts`, the GPU's ring cost in `sim_seats.py` and `sim_minors.py`, and every duration the install scales.
  - BUILD: a seat's government is picked, not computed. `computeAdoption` / `_adopted_gov` take the newest tier with ties to table order, so Oligarchy and Classical Republic are adopted in no game. A `government` field on the seat's action record, validated by the applier, chosen by the driver; the newest-tier pick becomes the validator's default. Shares A-53's second government path.
  - LAB B-D-S0 / S1: the per-city split. `GlobalParameters.xml` carries twelve `WAR_WEARINESS_*` rows and no pack adds one; `LOSS_OVER_REQ_AMENITIES_{AT_WAR_CITY 3, FOUNDED_CITY 0, NONFOUNDED_CITY 1}` may be caps below the city's need rather than addends. The empire-wide rule ships (`warWearinessPenalty` over `wwMax`).
- **B-82. THE TURN-LIMIT SCORE WINNER.** Weight 2.
  - BUILD: past `TURN_LIMIT` TS `endTurn` names nobody and the GPU names `leader()` over `seat_score`, the env's yield score. Build Civ 6's Score on both engines from `Scoring.xml` <- `Expansion1_Scoring.xml` as measured (lab 4, `score_read.lua` / `score_fit.py`, `runs/score_obs2_20260924.txt`): civics 3 each, techs 2, wonders 15, Great People 5 per person earned, era score 1 per point, Empire 5 per city + 2 per completed district + 1 per citizen, Religion 5 per belief of the seat's religion. The highest total wins; `TieBreakerPriority` orders ties. The env reward stays its own number.
  - LAB B-82-S1: Empire's `LINE_ITEM_ERA_BUILDINGS` residue (10–43; neither every building nor an era subset fits) and Religion's `Converted` term (the leading reading: other players' cities following it, minors included). A zero-turn re-read of the saved games.
- **B-85. EXODUS OF THE EVANGELISTS' GOLDEN MOVEMENT.** Weight 1.
  - BUILD: `COMMEMORATION_RELIGIOUS_GA_MOVEMENT` (+2) takes `UNIT_IS_GOLDEN_AGE_RELIGIOUS` = `UNIT_IS_RELIGIOUS` (Missionary, Apostle, INQUISITOR — `Expansion1_Moments.xml`) and the golden age; the text: "+2 Movement for all Missionaries, Apostles, and Inquisitors". Add the Inquisitor to `goldenMoveBonus` (`cpu/core/eras.ts`) and to `_golden_move_mp`'s tuple (`gpu/core/sim_masks.py`).
- **B-86. THE EMERGENCY COMBAT TERMS' REACH.** Weight 1.
  - LAB B-86-S0 / S1: the lost Nuclear emergency's `EFFECT_ADJUST_CITY_IDENTITY_PRESSURE` -1 ("Member cities exert one less Loyalty pressure") is read as each member city pressing as one fewer citizen (`citizenPressure` / `_citizen_pressure_from`); the install carries a separate per-citizen effect, which argues for a city-level term. Per-source pressures (`GetCityIdentityPressures`) around a lost emergency decide it; raising an emergency in the lab is itself unproven (S0).
  - LAB B-86-S2: the anti-air answer to a sortie carries no seat-pair term on either engine; the emergency terms require `COMBAT_UNIT_VS_UNIT`, and whether the AA burst is one is read by preview.
- **B-87. THE MARAE'S DEDICATION EVENT.** Weight 1.
  - BUILD: Pen, Brush and Voice's building half is `EFFECT_ADJUST_PLAYER_ERA_SCORE_PER_CULTURE_BUILDING_CONSTRUCTED` with no building argument, and its only statement of scope is "+1 Era Score for constructing a building with a Great Work Slot". The Marae has no `Building_GreatWorks` row, yet both engines fire it because it replaces the Amphitheater (`buildingDedications` over `GW_HOLDERS`, the GPU's `_b_gwslot`). Fire only for a building the seat's own variant gives a slot.
- **B-89. FIRE ON A RELIGIOUS UNIT.** Weight 1.
  - LAB B-89-S1: both engines let a melee, ranged, city or barbarian attack take a Missionary or Apostle (`attackTargets` / `hostileRangedStrike`; `_civclass_at` / `_nonbarb_unit_plane`). The pedia: "Religious units are also vulnerable to attack from combat units. Condemn Heretic is an action that combat units may take against them when on the same tile"; both engines condemn from an ADJACENT tile (`A_CONDEMN` + direction). The legality readers answer both.
- **B-90. CITIZEN LOYALTY PRESSURE.** Weight 1.
  - BUILD: the pedia (`Expansion1_Civilopedia_Text.xml`): "Each Citizen exerts a base pressure of 1 … Citizens in a Capital city exert an additional 1 pressure. Golden and Heroic Ages add 0.5 for all Citizens, while Dark Ages subtract 0.5"; `CITIZEN_IDENTITY_PRESSURE_BASE` 1 and `_CAPITAL` 1 (every layer). Both engines press population × distance weight with no capital term and scale the whole by an age FACTOR (`citizenPressure` × `agePressureFactor`, `_citizen_pressure_from`). Per citizen: base + capital + age, both engines.

## C. Absent systems — the blockers, and the gaps waiting on them

- **C-1. POWER.** Weight 1.
  The NUCLEAR ACCIDENT ships on both engines as measured (`nuclearAccident` / `_nuclear_accident`, `ACCIDENT_*` in `cpu/data/disasters.ts`): three rows of the turn's one event draw (C-74) at `OccurrencesPerGame` 1, one site per major city whose reactor age (`City.reactorAge` / `city_reactor_age`) has reached the row's `MinTurnAtRisk` 10 / 20 / 30; two draws, the Industrial Zone pillaged at DISTRICT_PILLAGED's 0 / 50 / 100, ONE citizen lost at POPULATION_LOSS's 0 / 0 / 80 (never the last), RADIATION_LEAKED's 2 / 10 / 20 turns of fallout on the Industrial Zone's plot alone; no ring improvement pillaged, the plant kept and ageing. REACHED by `tests/cpu/map/disasters.test.ts` and the `power` poke lane; the serve gate reaches it only past Nuclear Fission.
  - MEASURED, lab 4 (`tools/civ6lab/reactor_fleet.py`, `runs/reactor_20260923T192129Z.jsonl`): 75 forced accidents, 25 per severity: the Industrial Zone pillaged 0/25, 13/25, 25/25; one citizen lost in 22/25 cities at CATASTROPHIC, none at MINOR (the record's seven MINOR losses came after a turn: a socket-set population snapping back, and two turns passing); the fallout on the reactor's own plot, 2 / 10 / 20 turns; no building removed (0/80) and no farm within 3 pillaged (0/40).
  - BUILD: a Free City's reactor keeps ageing and is an accident site — a flip keeps the Power Plant, but `eventSites` walks `state.seats` only and `_reactor_plane` slices majors. Age and draw every reactor whatever its owner.
  - LAB C-1-S1: BUILDING_PILLAGED (MINOR 20, MAJOR 100) — lab 3's InGame read, taken correctly, pillaged the Factory and Power Plant and spared the Workshop at MAJOR, so per building or the plant alone is open (the "`IsPillaged` throws" reading was the `ok and v or "err"` idiom printing a false); and the UNIT_DAMAGE / UNIT_KILLED_CIVILIAN / CITY_GARRISON rows (MAJOR 50, CATASTROPHIC 100, damage 20–50), which neither engine applies.
  - BLOCKER C-38: a city-state's cities are never powered (`resolveSeatPower` / `_resolve_seat_power` run for majors only); vacuous until its build order reaches a building with a load (the watched minors build Factories, Research Labs and Broadcast Centers), pinned by `minor_yields_test::test_power_vacuous`.
- **C-2. DIPLOMATIC AGREEMENTS.** Weight 1.
  The promises ship on both engines: the four `DiplomaticActions_XP2` rows (`cpu/data/promises.ts`, `eras.promises`), a ledger per ordered pair (`promiseWith` / `seat_promise`, `promiseBrokenWith` / `seat_promise_broken`), the ask and the answer settled in one turn (`settlePromises` / `_settle_promises`), the incursion (`promiseIncursion` / `_promise_incursion`) called from the offensive spy mission, the city that comes to follow the promiser's religion and the dig worked on the asker's ground, and the War of Retribution's `brokenPromise` reading the window. The plain "settled too near" grievance ships (`grievanceSettledNear` / `_grievance_settled_near`, from `foundCityAt` / `_found_city_at`): every other major owning a plot within 3 of the new centre gains it against the founder. REACHED by `tests/cpu/seats/promises.test.ts`, `grievances.test.ts` and the `promises` poke lane; the serve gate through the driver's `_promise_turn`.
  - BUILD: `GRIEVANCE_SETTLED_NEAR` is 25, not 19. The lab read 19 and 18 one turn after the act, after the Industrial (6) and Renaissance (7) `GrievanceDecayRate` (`Expansion2_Eras.xml`); the decays read (19→13→7, 18→11→4) and the city-state conquest's 50 read as 42 / 41 confirm the lag.
  - LAB C-2-S1 (ask 18): the break's reach. The watches never saw a break — every promise lapsed after about 30 turns — and the founding scene tried border distances 3–6 only. Found at border 1 and 2 with the promise standing.
  - LAB C-2-S1: `GRIEVANCE_MULTIPLIER_FOR_BROKEN_PROMISE` 200 names no operand; both engines charge the notification's literal 100. The grievance log (`GetGrievanceLogEntries`) read in the turn of a break gives the amount.
  - Model lines, identical on both engines and kept as notes: a luxury has no lump to trade (the install trades ACCESS, never an amount); the intel bonus is unit-against-unit only; one running deal per ordered pair, `DEAL_ITEMS` a side, an offer standing two turns; a war does not end a standing deal or promise; the Third Party War's "another player" is read as an ALLY; a refused ask stands the pedia's 30 turns; the refused asker's favor comes back at the refusal, where the pedia says "refunded for the next session"; an offensive mission is an incursion whatever its outcome; an ask is answered in the turn it is made; an ask the asker cannot make is dropped, neither paid nor refused.
- **C-16. THE SPY'S SECOND HALF.** Weight 1.
  - The escape ships on both engines as measured (`spyEscape` / `_spy_escape`; lab 4, `spy_loop.py`, 129 escapes): the police guess one offered route uniformly, and the spy gets away when the 3d6 lands at or under 10 + its level (-4 on a right guess). One-route cities let 10 of 54 away (14.4 predicted); two-route cities 41 of 63 (32.9 predicted, about +2σ); a lost escape ended in the cell 34 times in 117.
  - BUILD: a counterspy guards its district "and all adjacent districts" (`LOC_ESPIONAGECHOOSER_COUNTERSPY`); `counterspiesGuarding` / `_counterspies_guarding` guard the post's own tile alone (or the city with Surveillance), and take the first post by slot rather than the highest level.
  - LAB C-16-S1: `ESPIONAGE_ESCAPE_COUNTERSPY_LEVEL_MODIFIER` -1 per level is applied as written, unmeasured: all 234 prompts named the police. A counterspy posted by a second seat, read at the prompt.
- **C-20. THE ROUTE'S TRANSPORTATION EFFICIENCY.** Weight 2.
  - BUILD: a route's path. The pedia: a route's yields are multiplied by its path "over water or Railroads, or through Canals or Mountain Tunnels"; the rows are `TRADE_ROUTE_TRANSPORTATION_EFFICIENCY_MAX_RATIO` 1.0, `_SCORE_BEST_ROUTE_TILE` 2, `_SCORE_MULTIPLE_DOMAINS` 15, `_SCORE_PORTAL_USE` 15, `_SCORE_WATER_TILE` 2 (`Expansion2_GlobalParameters.xml`). Both engines' trade walk treats every Mountain as impassable and never takes a portal (`tradeWalkable` / `tradeWalkStep`, where units use `portalExit`). Walk routes through Mountain Tunnels (`MOUNTAIN_PORTAL`: the Tunnel and Qhapaq Ñan) and register the five rows as sourced constants.
  - LAB C-20-S1: how the score becomes the multiplier on the path's gold (Σ/100 capped at `MAX_RATIO`, or normalised by length; floor or round). The route-chooser previews give it exactly; until then no path term applies.
- **C-26. CIVILIZATION ABILITIES — THE RESIDUE.** Weight 2.
  The census is `docs/roster_ledger.json`, read as `docs/ROSTER.md` says (`shipped` on 341 of 343 modifiers, two `AI:` rows).
  - BUILD: a city's own ranged strike composes its defender without the roster's rows (`cityStrikeDefenderCS`, `_seat_city_strike`; allowlisted in `combat-rows.test.ts` / `combat_rows_test.py`). Add the roster term — `rosterCS` / `_roster_cs` first need an attacking / defending sense — and delete both allowlist entries.
  - BUILD: Qhapaq Ñan (`IMPROVEMENT_MOUNTAIN_ROAD`, `TRAIT_LEADER_PACHACUTI_IMPROVEMENT_MOUNTAIN_ROAD`) is in neither engine's catalog while Pachacuti is in the roster.
  - BUILD: the census omits the thirteen `TRAIT_FLOODPLAINS_VALID_*` rows (twelve in Base `Civilizations.xml`, `…_GOVERNMENT` in `Expansion1_Civilizations.xml`); add them with their reading.
  - LAB C-26-S1: whether Trajan's grant (`TRAIT_ADJUST_NON_CAPITAL_FREE_CHEAPEST_BUILDING`, no requirement set, no RunOnce) fires on a CAPTURED city; founding ships (`trajansColumn` / `_trajans_column`).
  - LAB C-26-S3: whether Norway's Ocean clause (`TRAIT_EARLY_OCEAN_NAVIGATION`, Ocean valid after Shipbuilding) reaches a Trader's water path before Cartography (`tradeWaterLevel`).
- **C-34. AIR COMBAT'S SECOND HALF.** Weight 3.
  - BUILD: PATROL is `UNITOPERATION_DEPLOY`. The pedia's Air Combat chapters: a deployed fighter holds a hex and intercepts within 1 until ordered otherwise; stationed aircraft do not intercept; the strongest interceptor fights and each other adds +5; an intercepted fighter aborts its attack while a bomber continues. Both engines answer a sortie with AA units and ships only (`airCoverAnswer` / `_air_cover_answer`). A deployed air state on both engines, its record arm and the driver's choice.
  - BUILD: PRIORITY TARGET: "attack Support class units directly, without first having to eliminate the enemy combat unit placed in the same location" (`UNITCOMMAND_PRIORITY_TARGET`). Both engines' air strike always takes the stack's combat defender (`airStrike` → `stackDefender(…, true)`, `_air_strike`). A priority flag on the air-strike record.
  - LAB C-34-S1: the interception's strength and damage by preview.
- **C-38. A CITY-STATE'S PLAY.** Weight 6.
  OWNER 2026-09-23: a city-state is no agent, so its behaviour IS the environment an agent trains against, and a gap an agent can exploit there is a sim-to-real gap. Every observable city-state behaviour is therefore an engine rule: built from the install where it publishes rows, measured in the lab where it does not, and randomised per episode where neither settles it, so no policy leans on one guessed value. Its food and culture already ride the majors' composers.
  - MEASURED, 262 watched games (`tools/civ6lab/runs/cs_watch_*.jsonl`: lab4, obs1–6, cfgA–E, s2n1–5; 35,081 city-state turns, every one an Ancient start; re-matched with the install's `UnitUpgrades` chains): every minor starts with a Settler and two Warriors. FIRST BUILD a Monument (213 of 250) or a Slinger (37); then a Builder (median turn 4), Warrior (16), Granary (20), Archer (29), Trader (35); walls in 230 of 250 at median turn 63; the type's district in 157 of 250 (Scientific 36/40 at 27.5, Religious 32/39 at 26, Cultural 29/42 at 58, Trade 26/40 at 57.5, Industrial 20/45 at 87.5, Militaristic 14/44 at 98.5), never another type's; Harbor, Water Mill, Castle, Star Fort, Neighborhood, Sewer and the district projects later; 21.7% of turns to t101 build nothing. TRAINED 2,321 units (Builder 636, Warrior 502, Archer 333, Trader 307 …). BUYS: 453 Builders (452 with no Builder standing, bank ≥ 93, median 115 spent; 3–12% a turn with a bank of 100–300), 203 military units (bank always ≥ 95, median share left 0.24; 1.7% a rich turn at 0–1 units, 0.4% at 6–7, 0 of 348 at 8+; tripled within 3 turns of a loss), 2,083 upgrades (about 4.3 gold each — `UPGRADE_MINIMUM_COST` 15 is the floor candidate — despite the 100% discount), faith 7 times in 262 games (Warrior Monks). Builders stay within 2 of the centre 94% of turns. Land military at peace: distance from the centre d0 8%, d1 21%, d2 22%, d3 18%, d4 15%, d5 11%, 6+ 5%; at war d0 17%, d1 25%, d2 24%, d3 13%; a turn's step 0: 19.5%, 1: 56.7%, 2: 20.6%, 3+: 3.2%.
  - SHIPPED, both engines: the Ancient starting army (two Warriors from `loadWorld`, riding the GPU's majors' pool under 100 + id), which holds, digs in, heals, defends and is removed with the minor's conquest; the walled minor's city strike (`cityStrikes` from `minorPhase`, `_city_strikes` from `_city_state_phase`); the production rows (`MINOR_CIV_PRODUCTION_PENALTY` -50% and the toward-rows, measured to MULTIPLY: x1 plain, x3 walls / Builder / military, x6 the type district; `minorProduction`, `toward`). REACHED by `minor-army.test.ts`, `minor-strike.test.ts`, `minor-record.test.ts`, the `minor_record` and `minor_builds` lanes and the serve gate.
  - BUILD, ITS PRODUCTION: a build order fitted to the census in place of the ladder (`minorBuild.ts` / `_minor_build`), with unit training (Builder, Warrior, Archer, Trader, then the unit line) through the shipped rows — the Builder and military rows (+200%, the latter under `PLAYER_HAS_SMALL_MILITARY`, fewer than 10 units) then reach something; `MinorCivDistricts` disfavours sixteen districts in a GS game, `MinorCivUnitBuilds` Recon and carriers, `MinorCivPseudoYields` tourism.
  - BUILD, ITS PURCHASES AND UPGRADES: a Builder bought when none stands and the bank covers it, a military unit bought at the measured rates by army size above a bank floor, upgrades on the tech / civic trigger (`MinorCivTriggeredTrees`) at the measured cost, faith spent on the rare Warrior Monk; `CityState.treasury` / `.faith` stop only banking. Rates where the data gives a rate are drawn per episode.
  - BUILD, ITS WALKER: a first fit of the measured distance and step distributions, peace and war, for the minor's land units on both engines (`minorPhase` / `_city_state_phase`); the same body serves the Free Cities (C-60).
  - BUILD: the quest's camp radius is 5 ("Destroy one Barbarian Outpost within 5 tiles of the city", `Quests_Text.xml`); both engines use 6 (`issueQuest`, `_seat_quest_phase`).
  - BUILD: the walls tier reads the minor's Urban Defenses research on TS (`wallsTier` through `seatOf`) and its buildings alone on the GPU (`_minor_walls_tier_at`); the GPU reads the research too, and both comments claiming otherwise go.
  - LAB C-38-S1: the quest pool (eight kinds in `Quests.xml`, no weights; `QUEST_COOLDOWN` 12 and `QUEST_ENVOYS` 1 unsourced), what builders improve, the research order, and unit tracks by id — an extended observer watch.
  - LAB C-38-S2: the centre strength. `minorCityCS` / `_minor_centre_cs` read 15 + population + 6 (militaristic) with no row; the install's only minor strength is `StartEras.StartingMeleeStrengthMinor` (25 at Ancient), and a garrison adds nothing where a major's centre takes +5.
  - BLOCKER, no start era: `BonusMinorStartingUnits`' later-era rows (Classical to Information, GS swapping Pikeman for Pike and Shot) wait on a later-era start existing on either engine.
  - BLOCKER C-1: its grid, once its build order reaches a building with a load.
- **C-41. VOLCANIC SOIL.** Weight 2.
  Both engines paint the RADIUS-1 RING as measured (`erupt` / `_erupt`): one draw per eligible plot in ring order (`soilPaintable` / `_soil_paintable` — land, no Mountain, no district, city centre or wonder, bare or Woods / Rainforest), Volcanic Soil at the severity's YIELD_FOOD row (35 / 50 / 75), replacing the Woods or Rainforest and a Lumber Mill; then they scorch (pillage) and fertilize the ring. The radius is sourced: `RealismSettings.ExtraRange` is false at MODERATE. REACHED by `tests/cpu/map/feature-add.test.ts` and the `feature_add` lane.
  - BUILD: the eruption applies only its pillage row. CATASTROPHIC and MEGACOLOSSAL also carry `IMPROVEMENT_DESTROYED` 75 / 80, `DISTRICT_PILLAGED` 75 / 80, `BUILDING_PILLAGED` 100, `POPULATION_LOSS` 20 / 35, `UNIT_KILLED_CIVILIAN` 20 / 35, `UNIT_DAMAGE_LAND` 40–60 / 60–80, `CITY_GARRISON` and `CITY_WALLS` (`Expansion2_RandomEvents.xml` `RandomEvent_Damages`). Apply them as the flood and storm rows are applied.
  - MEASURED, lab 4 (`volcano_scene.py`, `runs/volcano_20260923T191337Z.jsonl`, 78 eruptions): the paint is a per-plot chance applied at once — bare land 30 / 52 / 74%, Forest and Jungle 6/28, 11/28, 18/28, Floodplains and Geothermal Fissure never. The damage switched ON per plot as the run went on and then followed the rows (GENTLE pillage 3/3; CATASTROPHIC 8 destroyed + 1 pillaged; MEGACOLOSSAL 12 + 2); strategic and luxury resources were never lost (0/42), bonus resources on 21/21 painted and 9/21 unpainted plots.
  - LAB C-41-S1: what switches the damage on (plot ownership is the leading candidate: the scene laid improvements with owner -1); whether only bonus resources are lost and on which plots; whether Marsh and Oasis (`ValidForReplacement`) are painted; a district, centre or wonder on the ring; and the `YIELD_PRODUCTION` (15/25/35) and `YIELD_SCIENCE` (10/15) rows under Volcanic Soil, read by neither engine.
- **C-49. NAMED STORMS.** Weight 1.
  `stormWalk` / `_storm_walk` ship the measured model: eight unit steps on the movement turn from the `PrevailingWinds` band at the centre's current latitude, eight more with no footprint on dissipation.
  - LAB: the per-step law. The 31 resultants measured fit no single-heading walk, and the records carry a per-storm `CurrentDirection` set at spawn on the band's side and changing each turn — a persistent-heading walk is the alternative. No mid-turn read exists (the tuner runs between turn processing). First the natural storms' 16-step resultants from the save sweep (C-74-S1), then C-49-S1's batch hurricanes if they do not separate the laws.
- **C-60. THE FREE CITY'S OWN PLAY.** Weight 3.
  The seat is in on both engines (revolt, race, join, Eleanor's skip, open to attack, the religion walk). Its city stands on a flat 72 (`FREE_CITY_DEFENSE`; `holderStrength` / `_holder_base`, measured once, Medieval, no walls, no garrison); a walled Free City fires the ordinary strikes; the centre heals at the ordinary rate and its loyalty restarts at 100. REACHED by `tests/cpu/city/free-city.test.ts` and the `free_city` lane.
  - MEASURED, 28 flips over the watched games (`cs_watch_*`, `rebel_watch_rebel3b/3c`): the revolt's pair is the era's melee (Swordsman, Man-at-Arms, Musketman, Line Infantry, Infantry, Mechanized Infantry — within a step of the minors' best melee at that turn); then a unit EVERY 5 turns while the city stays Free, of any class and the era's type (Horseman, Archer, Courser, Crossbowman, Cavalry, Helicopter, Field Cannon, Machine Gun, Musketman, Line Infantry, Infantry, Ranger, Mechanized Infantry, and Builders), on the same tile; it TRAINS (Slingers, Warriors, a Trebuchet; walls, a Castle, districts and repair projects in its queue); its player holds gold (0 on 223 of 296 city-turns, up to 120, falling 4–5 a turn with upkeep); its units move (d0 5%, d1 60%, d2 28%, d3+ 7%; a turn's step 0: 48%, 1: 17%, 2: 17%, 3: 14%); on a join the granted units near the city vanish the same turn, the trained ones stay Free.
  - BUILD: BANKRUPTCY, for every seat. `GOLD_NEGATIVE_BALANCE_AMENITY_LOSS_LINE` 0, `_DISBAND_UNIT_LINE` -10, `_SUBSEQUENT_AMENITY_LOSS` -10, `_SUBSEQUENT_DISBAND_UNIT` -10 (Base `GlobalParameters.xml`); the pedia: "-1 penalty to your Amenities per every 10 Gold you drop below 0 … at -10 Gold you will automatically disband a unit, at -20 two units"; the getter is per city (`GetAmenitiesLostFromBankruptcy`). Both engines disband the priciest unit at any balance below 0 (`phase.ts` upkeep block, `_bankrupt_disband`) and read none of the four rows.
  - BUILD: the Free Cities player's play from the measurements above — the era's melee pair at the revolt, the recurring grant every 5 turns of the era's type, its training and build queue, a treasury with upkeep (and so bankruptcy), and the walker C-38 builds (`flipCity` / `_seat_loyalty_flips`, `grantFreeCityUnit` / `_grant_free_unit`, `freeCitiesPhase` / `_free_cities_phase`); on a join its granted units near the city are removed.
  - LAB C-60-S1: whether the 72 follows the era, and whether walls and a garrison add to it (zero-turn reads of the Free Cities standing in the saved games).
  - LAB C-60-S2: the flip pair's rule (world era, the former owner's best melee, or its own techs), the grant's type draw, the grants' fate on a join or fall (removed or transferred), its income, its walker's targets.
  - LAB C-60-S3: a grant with every ring tile occupied.
  - LAB C-60-S4: bankruptcy's shape over the turns of insolvency (per city or empire, whether the balance goes negative — no read below 0 in 35,000 rows — and which unit goes).
- **C-74. THE TURN'S ONE RANDOM EVENT, THE RESIDUE.** Weight 4.
  Both engines fire at most ONE random event a turn (`randomEvent` / `_random_event`, from `disasterPhase` / `_disaster_phase`): one draw over the eligible (row, site) pairs in the install's `RandomEvents` order, each pair weighted by its row's `OccurrencesPerGame` at MODERATE (`eventRows` / `_event_rows`) — the three floods once per river carrying Floodplains (`floodSites` / `_flood_sites`), Kilimanjaro's two eruptions once per Mount Kilimanjaro plot, the three eruptions once per volcano, the eight storms once each while their start terrain exists, the three nuclear accidents once per reactor past `MinTurnAtRisk` (C-1), and the two droughts once while Plains or Grassland exists. The drawn row IS the severity; a storm's or a drought's centre is a second draw. A warmed world moves weight between severity rows (`severitySplit`); the spy's breached Dam draws its flood's severity by the same weights (`floodSeverity` / `_flood_severity_draw`). REACHED by `tests/cpu/map/disasters.test.ts` and the `storms`, `feature_add`, `flood_severity` and `power` lanes.
  - MEASURED, the event histories (19 games, `runs/event_history_*`): one slot a turn; the empty share falls with map size (Small 25%, Standard 8%, Large 1%, Huge 0%) and one-site rows run at about `OccurrencesPerGame` × turns / 250 × 0.75 / 0.93 / ~1.2 by size — the column is an absolute per-site rate, not only a weight; lab 4's 74 eruptions fit 70% of its 13 volcanoes; the flood mix held at base through seven sea-level rises while the severity-2 storm share and drought EXTREME rose, as the `ChanceIncreasePerDegree` columns say; no strict 15-turn cooldown per row (19 CAT_4 in 249 turns).
  - BUILD: the drought's rules (`LOC_CLIMATE_DROUGHT_EVENT_DESCRIPTION_TOOLTIP` and `RandomEvent_PillagedImprovements` across the layers): it targets featureless areas ("Keep some Woods, Rainforest, or Marsh near your cities to lessen the chance"; `droughtCandidate` ignores features); it pillages Farm, Camp, Plantation, Pasture, Mekewap, Outback Station, Cahokia Mound, Terrace Farm and Hacienda (SPECIFIC_IMPROVEMENT_PILLAGED 100, EXTREME's SPECIFIC_IMPROVEMENT_DESTROYED 30), which cannot be rebuilt until it ends; `PreventsDrought` on the Aqueduct, Bath, Dam and Stepwell spares a city's food (`yields.ts` applies -1 food with no immunity).
  - BUILD: `RANDOM_EVENT_START_TURN` 2 (`Expansion2_GlobalParameters.xml`); both engines can fire on turn 1.
  - BUILD: the warmed world's severity shift is each row's `ChanceIncreasePerDegree` (floods 20 / 20 / 20, storms 0 on severity 1 and 50 on severity 2, drought MAJOR 0 and EXTREME 50, fires 50) per degree of warming (`Maps_XP2.CO2For1DegreeTempRise`, which `CO2_PER_POINT` matches), in place of `severitySplit`'s ice-melt shift.
  - BUILD: the pack rows that load in every Gathering Storm game on the owner's install (OWNER 2026-09-25: in scope): `METEOR_SHOWER`, `FOREST_FIRE`, `JUNGLE_FIRE` (`GranColombia_Maya_Expansion2.xml`, criteria `RULESET_EXPANSION_2`: burning, burnt and regrowing features, spread, the meteor's goody improvement, `AvoidTerritory`), Eyjafjallajokull's eruptions (`VikingsLandmarks_Expansion2.xml`) and `VESUVIUS_MEGACOLOSSAL`, with their wonders where the roster lacks them. Only the `*_MODE.xml` files (Apocalypse) stay out.
  - LAB C-74-S1 / S2: the empty turns — the normaliser and the map factor, measured at Duel, the engine's own size; what a site is (active volcanoes under `PercentVolcanoesActive` 70; a river or a floodplain plot); `Spacing` 15 (a distance or a gap); `RANDOM_EVENT_FIRST_TIME_OCCURRENCE_BOOST` 30.

## Harness — not weighted

- **A-51. SEAT 0 WHERE THE INSTRUMENTS DO NOT LOOK.**
  - BUILD: `SimInit` fills ROW 0 of the city planes differently (`_base[:, 0].fill_`: `city_center` -1 and `city_hp` `cityMaxHp` where the other rows start at 0). No reader needs it; make every row start alike.
  - BUILD: `createGame({opponents})` (`cpu/core/game.ts`) opens a game as seat 0 plus opponents through `placeSeats` (`cpu/core/phase.ts`) and `placeCityStates` (`cpu/core/cityStates.ts`) — placement paths beside `seeder/place.ts` — and `createGameFromMap` pre-creates seat 0 (`emptySeat(0)`, `loadWorld` back-fills the rest). 22 test files and `tools/cpu/perf-turns.ts` are its only users: move them to seeded worlds and delete it.
  - BUILD: `Seat.aggression` is a driver preference weight stored as engine state (`cpu/core/types.ts`, `world/file.ts`, `seeder/place.ts`, `phase.ts`, `world/load.ts`, `seats.ts`, `cpu/export/planes.ts`, read by `cpu/core/observe.ts`; the GPU's `civ_aggression`, read by `env.py`). No engine rule reads it; the driver owns it.
  - BUILD: `tools/gpu/seat_symmetry_check.py`'s `FORK_PATTERNS` miss the `[:, 0]` fill, default-0 seat parameters (`env.py`, `sim_orders.py` `row: int = 0`), `observe(0)` in `serve_gate.py` and `emptySeat(0)`; its `c["seat"]) == 0` form no longer exists. Teach it the live forms.
- **A-52. SWITCHES THAT ARE ALWAYS ON, AND RULE READS WITH DEFAULTS.**
  - BUILD: `GOVERNMENTS_ADOPTION_LIVE`, `CITY_RELIGION_ADDER_LIVE`, `BARB_SCOUT_OPENER_LIVE`, `ADMIRAL_MARCH_LIVE`, `DEDICATION_PAYOUTS_LIVE`, `ENGINEER_LIVE`, `SCRIPTED_CAMPUS`, `B18_FOLLOWER_COUPLING_LIVE` and `embarkState.live` are all `true` in play (`setEmbarkLive` and two GPU tests' `_gov_has_effects = False` switch arms off for tests only). Delete each flag, its arms, its key and its switch-off test.
  - BUILD: keys exported and applied by neither engine: `admiralMarchLive`, the campus scaffold's `active` and `campusUnlockTech`, and `formalWarMinTurns` (the GPU reads it into `_formal_war_min` and never uses it; both engines take the formal war's 5 turns from `WAR_KINDS`). Stop exporting them and delete the reads; the other war kinds' literal 5s read `FORMAL_WAR_MIN_TURNS`.
  - BUILD: the GPU's `improvements_on` / `districts_on` gate sections the exporter always ships, and about 590 `.get("key", <number>)` rules reads in `gpu/core` (about 167 with a real magnitude) carry a second copy of each value. Index the rules directly so a missing key fails loudly.
- **A-53. THE TS VERB LAYER ONLY TESTS DRIVE.**
  - BUILD: `cpu/core/game.ts`'s `queueSettler`, `queueDistrict`, `queueBuilding`, `queueWonder`, `purchaseBuilding`, `purchaseUnit`, `cancelQueueItem`, `setTechResearch`, `setCivicResearch`, `setGovernment`, `setPolicy`, `toggleLockedTile`, `choosePantheon`, `foundReligion`, `enhanceReligion`, `placeImprovement`, `removeFeature`; `cpu/core/units.ts`'s `orderMove`, `queueUnit`, `builderImprove`, `builderRepair`, `unitMaintenance`, `setExploreMission`; and `spreadReligiousPressureForTest` have no engine caller. `setGovernment` is the only writer of `government.current`, which keeps a second government path alive in `governmentSlots`. Move those tests onto the action record and the applier, then delete the layer and `government.current` (B-D's government choice is the record's field).
  - BUILD: `serialize` / `deserialize` back-fill old save shapes (`growthBox`, `legacyCamps`) that no test exercises; the re-exports `GENERAL_AURA_CS` / `GENERAL_AURA_RANGE` (`combat.ts`; importers `export/rules.ts`, `great-general.test.ts`) and `nextRandom` (`units.ts`; importer `units.test.ts`); `policy/ladder.py`'s `_best_in_lane` has no caller. Delete each.
