# A-25 + G-8 round — captured-city luxuries + the cap-32 dormant divergence

2026-07-20. Owner-picked pair. SERIAL main-session stages (both touch
the amenity path; S1's fix is S2's prime suspect). ONE battery at the
round end. Brief-first per the standing rule.

## Unification hypothesis (drives the stage order)

G-8's sighting is an ECONOMIC drift at the −3/−4 amenity tier (seed
9092 t216, player war, cap-32 experiment). A-25 is a MISSING amenity
source (captured-city luxuries absent from the GPU empire pool). A
missing luxury shifts the amenity balance by exactly the kind of
offset that flips tiers in the deep-penalty regime the cap raise
newly exposes. Fix A-25 FIRST, then re-verify G-8 — it may dissolve.
If it does not dissolve, hunt it fresh (its tier attribution is
agent-diagnosed and UNVERIFIED — the G-3 rule).

## S1 — A-25: captured-city luxuries → the empire amenity pool

- Restore the recorded repro: `SEED_OVERRIDES[15]` 9197→9196
  (export-gpu.ts) — the B8-K reshuffle where the scripted player
  CONQUERS a rival city (acquired t7) at ~t240 and the GPU computes
  its amenity balance −4 (`amen_have` 0) vs TS −2 (growth 0.70 vs
  0.90, foodBox/pop drift).
- Export, scripted parity → expect index 15 red ~t240. Hunt via
  ckpt-resume statelog (the turn is KNOWN — no full --log run).
  RE-VERIFY slice K's probed root before fixing: "the captured
  city's tiles/luxury resources do not feed `_luxury_amenities`" —
  note `_capture_rival_city` DOES write the `owner` plane on the
  A-17 ring, so the failing predicate is NOT obviously ownership;
  suspects: the `improvement == lux_req` match on rival-improved
  luxury tiles (do rival builders write the resource's OWN
  improvement?), the `lux_id`/`lux_req` exporter bake vs the live
  planes, or a razed-vs-captured branch.
- Source of truth: real Civ 6 — captured luxuries DO transfer with
  territory and amenity-feed the empire. TS is presumed right; fix
  the GPU unless the hunt proves otherwise.
- Bar: seed 9196 green at 250t + full ladder. KEEP 9196 in-gate if
  all 24 seeds stay green (a scripted conquest-with-luxuries seed is
  standing coverage); otherwise re-park 9197 with the fix proven at
  the repro and say so in the close-out.

## S2 — G-8 verify + B-15 close: WAR_WEARINESS_CAP 16→32

- One-source change: `WAR_WEARINESS_CAP` 16→32 in src/data/rivals.ts
  (the GPU reads `warWeariness.cap` from rules.json — no engine.py
  edit for the constant itself). PER_AMENITY stays 8: the ceiling
  becomes −4 (the B-15 player-facing target); the rival↔rival
  SURPRISE ×2 keeps −1-per-4-turns to the same −4 ceiling.
- Export, scripted parity. If seed 9092 (or others) red: ckpt-resume
  hunt at the named turn; check the TS accumulator-vs-penalty clamp
  symmetry (`warWearinessPenalty` clamps via Math.min at read; the
  GPU clamps the ACCUMULATOR at write — under cap 32 these must
  still agree on every path, including decay re-entry).
- The cap raise is a BEHAVIOR change: trajectories reshuffle, seeds
  may structurally die (reroll + document), poke lanes may need
  test-side fixes. Budget a hunt.
- Bar: full ladder green. Closes G-8 (verified root recorded) and
  B-15 → 100%.

## Close-out

Sweep ALL poke lanes STANDALONE after the final re-export (the #55
S4 / verify-loop-cost catch-7 rule), then ONE battery --no-eval.
AUDIT: A-25 RESOLVED, G-8 RESOLVED (with the verified root), B-15 →
100%; completion table + chronology. HANDOFF/memory, push.

## Standing rules

Gates ladder per stage (tsc → touched vitest → export READ → scripted
→ forced → rollout); draw-count discipline (both stages must be
ZERO-DRAW — amenity/ww math rolls nothing); every yield-bearing write
bumps _eff_version; _MUTABLE/dtype for any new state (none expected);
AUDIT anchors by SYMBOL; commit -F with the standard trailers.
