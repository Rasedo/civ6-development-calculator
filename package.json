{
  "name": "civ6-development-calculator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "preview:map": "vite-node scripts/preview.ts",
    "rl:train": "vite-node scripts/train.ts",
    "rl:eval": "vite-node scripts/evaluate.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
