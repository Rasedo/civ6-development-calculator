# PyTorch: for GPU on Windows/NVIDIA install FIRST with the CUDA index, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# (plain `pip install -r requirements.txt` pulls the CPU build, which also works)
torch>=2.2
gymnasium>=0.29
stable-baselines3>=2.3
sb3-contrib>=2.3
numpy>=1.24
tensorboard>=2.15
tqdm
rich
